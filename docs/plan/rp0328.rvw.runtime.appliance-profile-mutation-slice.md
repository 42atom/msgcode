---
owner: user
assignee: codex
reviewer: user
why: 记录 0328 这刀只实现 profile mutation，不让 settings 写面一下子铺开
scope: 只记录 `appliance set-profile` 的实现与回归锁；不讨论 UI 样式
risk: medium
accept: 留下稳定代码证据与测试证据，证明当前 mutation 仍然收在 profile 一组字段里
links:
  - /Users/admin/GitProjects/msgcode/issues/tk0328.rvw.runtime.p1.appliance-profile-mutation-slice.md
  - /Users/admin/GitProjects/msgcode/src/cli/appliance.ts
  - /Users/admin/GitProjects/msgcode/test/p7-tk0322-appliance-set-profile.test.ts
---

# RP0328：appliance profile mutation 切片

## 结果

新增：

- `msgcode appliance set-profile --workspace ...`

位置：

- `/Users/admin/GitProjects/msgcode/src/cli/appliance.ts`

## 这刀做了什么

1. 只开放四个显式字段：
   - name
   - organization-name
   - city
   - soul
2. 只写三份真相：
   - `.msgcode/config.json`
   - `.msgcode/ORG.md`
   - `.msgcode/SOUL.md`
3. 写后立即回读 `workspace-profile`
4. 中途失败时返回：
   - `changedFiles`
   - 当前回读到的 profile

## 这刀没做什么

- 没做通用 settings writer
- 没做事务
- 没做回滚
- 没扩到 general/capabilities/doctor

## 验证

- `PATH="$HOME/.bun/bin:$PATH" bun test test/p7-tk0322-appliance-set-profile.test.ts`
- `./node_modules/.bin/tsc --noEmit`
- `npm run docs:check`

