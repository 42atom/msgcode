---
owner: codex
assignee: codex
reviewer: user
why: archive 读面已经实现，需做一次轻圆桌确认是否有做重或偏离 file-first
scope: 只审 appliance archive 读面与其 UI/文档关系；不扩展 restore
risk: low
accept: 存在一份简短审议结论，确认这刀仍然保持单一主链和降级诚实
links:
  - /Users/admin/GitProjects/msgcode/issues/tk0347.rvw.runtime.p1.appliance-archive-read-surface-slice.md
  - /Users/admin/GitProjects/msgcode/docs/plan/rp0347.rvw.runtime.appliance-archive-read-surface-slice.md
---

# RP0348：appliance archive 读面圆桌审议

## 结论

这刀可以过。

## 共识

1. 读面足够薄
- 只读 `.archive/` 和 `archived-threads/`
- 没补 archive 索引表
- 没补 session archive

2. 降级口径是诚实的
- 根目录没了就报错
- 子目录没了就空
- 坏项只给 warning

3. 没抢 restore 的工作
- restore 仍留在 `0338 / 0340`
- 当前只补读取，不做移动

4. 和主界面的关系清楚
- 主界面只看活跃面
- archive 页只看这条读面
- 不让 UI 自己再猜归档状态

## 建议

下一刀如果继续，只做 restore 合同或 restore 实现。

不要把 archive 页长成第二后台。

