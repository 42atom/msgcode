---
owner: user
assignee: codex
reviewer: user
why: 当前已经决定 0267 是主线第一顺位，需要一份今天就能执行的最小切片，避免继续停在总 plan 层
scope: 只定义 0267 的最小执行顺序、触达文件和验收方式；不改 UI、不改 graph、不扩交付线
risk: medium
accept: 仓库内存在一份清晰的 0267 执行切片，说明今天先做哪几刀、各自落在哪些文件、怎么验
links:
  - /Users/admin/GitProjects/msgcode/issues/tk0275.tdo.agent.p0.fast-feedback-execution-slice.md
  - /Users/admin/GitProjects/msgcode/docs/plan/pl0267.pss.agent.deterministic-feedback-harness-for-coding-lane.md
  - /Users/admin/GitProjects/msgcode/issues/tk0267.tdo.agent.p1.coding-lane-fast-feedback-contract.md
  - /Users/admin/GitProjects/msgcode/issues/tk0268.tdo.runtime.p1.verify-pack-normalization-for-coding-lane.md
  - /Users/admin/GitProjects/msgcode/issues/tk0269.tdo.runtime.p1.verification-evidence-writeback-for-fast-repair-loop.md
---

# PL0275：0267 快反馈闭环执行切片

## 结论

下一步不要再讨论新方向，直接做三刀：

1. 先收 `tk0267` 合同层
2. 再收 `tk0268` verify pack
3. 最后收 `tk0269` 证据写回

一句话：

- 先让 Agent 默认会“改完就验”
- 再让 runtime 明确“该跑哪组验证”
- 最后让失败/成功结果有固定证据面

## 为什么今天先做这三刀

当前仓库并不是从零开始。

已经有：

- `prompts/agents-prompt.md`
- `AGENTS.md`
- `msgcode verify run`
- 任务文档里的 `Verify` 合同
- 既有验证合同 `tk0232`

所以现在不需要再造框架。

真正缺的是：

1. 合同不够硬
2. verify pack 边界不够清楚
3. 最后一次验证结果没有成为默认修复支架

## 今日执行顺序

## 第一刀：TK0267

### 目标

把 coding lane 的默认动作写死为：

- 改代码
- 立即验证
- 看真实错误
- 再修

### 直接触达文件

- `/Users/admin/GitProjects/msgcode/prompts/agents-prompt.md`
- `/Users/admin/GitProjects/msgcode/AGENTS.md`

### 只改什么

- 明确“代码任务默认进入快反馈循环”
- 明确“验证不是可选附加动作，而是默认下一步”
- 明确“停止条件只保留安全 / 预算 / 物理边界”
- 对齐 `tk0266`：失败默认看真实错误，不回到系统代答

### 不改什么

- 不写自动 retry
- 不写 orchestrator
- 不改 runtime

### 今天的验收

只要这两份合同读起来已经能明确推出：

- 代码任务默认要验证
- 验证失败后继续看真实错误并再修

就算第一刀成立。

## 第二刀：TK0268

### 目标

把 coding lane 的验证收成统一最小 verify pack。

### 直接触达文件

- `/Users/admin/GitProjects/msgcode/src/cli/verify.ts`
- `/Users/admin/GitProjects/msgcode/docs/testing/feishu-live-smoke.md`
- 必要时补：
  - `/Users/admin/GitProjects/msgcode/README.md`
  - 验证相关测试文件

### 要收成什么

统一口径至少覆盖：

- `test`
- `types`
- `e2e`
- `custom verify`

并明确：

- `e2e` 在本仓库里包括 browser/live smoke
- `custom verify` 继续尊重 issue 文档里的显式声明

### 今天最小实现建议

不必先做大平台。

先做最小收口：

1. 明确 verify run 的结果结构已经足够当基础
2. 再补一个“coding lane 默认跑哪些面”的正式口径
3. 必要时只加最小 wrapper/alias，不再造测试编排层

### 今天的验收

能明确回答：

- 代码任务默认先跑哪一层
- 什么时候要上 `e2e`
- 什么时候只跑 `custom verify`

## 第三刀：TK0269

### 目标

把最后一次验证结果写成下一轮修复可直接读取的证据。

### 直接触达文件

- `/Users/admin/GitProjects/msgcode/src/cli/verify.ts`
- `/Users/admin/GitProjects/msgcode/src/runtime/work-continuity.ts`
- 相关验证测试

### 最小字段

至少保留：

- command
- exitCode
- durationMs
- stdoutTail
- stderrTail
- fullOutputPath
- error

### 原则

- 只记录
- 不裁决
- 不写历史聚合平台

### 今天的验收

一轮验证后，下一轮修复已经能直接看到：

- 最后跑了什么
- 失败在哪里
- 详细输出去哪看

## 今天真正该怎么动手

如果只按最小行动走，顺序应该是：

1. 改 `prompts/agents-prompt.md`
2. 改 `AGENTS.md`
3. 读 `src/cli/verify.ts` 和现有 verify 测试，确认已有字段
4. 给 `0268` 收一个最小 verify pack 口径
5. 给 `0269` 把证据字段和写回位置固定住
6. 跑一条真实修复链验证

## 真实修复链的验收口径

本轮不是“文档好看”就算过。

必须至少跑通一条真实链：

1. 选一条小的代码改动
2. 改代码
3. 跑 verify
4. 看到真实失败或成功结果
5. 结果有标准证据面

只要这条链成立，`0267` 主线就开始真正落地了。

## 这轮不该做的事

- 不碰 `0259`
- 不碰 `0270`
- 不碰 Electron 壳
- 不碰 sidecar 站点
- 不做新控制层
- 不做自动修复平台

## 下一步判断标准

如果这三刀做完后，已经能稳定形成：

- 改代码
- 立即验证
- 看真实错误
- 再修
- 有证据回写

那主线就可以进入：

- `0259` 的纯文本工作态试跑

在这之前，不再开新线。
