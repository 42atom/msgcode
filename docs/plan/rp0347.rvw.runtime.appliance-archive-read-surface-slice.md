---
owner: codex
assignee: codex
reviewer: user
why: archive 原型页已经存在，需要一份实现报告把 runtime 读面、降级口径和 warning 口径钉死
scope: 只记录 appliance archive 读面实现；不记录 restore 设计
risk: low
accept: 读面实现、测试范围和限制条件说清楚，后续能直接接主界面或 archive 页
links:
  - /Users/admin/GitProjects/msgcode/issues/tk0347.rvw.runtime.p1.appliance-archive-read-surface-slice.md
  - /Users/admin/GitProjects/msgcode/src/runtime/workspace-archive.ts
  - /Users/admin/GitProjects/msgcode/src/cli/appliance.ts
  - /Users/admin/GitProjects/msgcode/test/p7-tk0338-appliance-archive-contract.test.ts
---

# RP0347：appliance archive 读面切片

## 结果

已落一条正式 archive 读面：

- `msgcode appliance archive --workspace <labelOrPath> --json`

对应实现：

- `/Users/admin/GitProjects/msgcode/src/runtime/workspace-archive.ts`
- `/Users/admin/GitProjects/msgcode/src/cli/appliance.ts`

## 真相源

这条读面只读两处：

1. 工作区归档根
- `/Users/admin/msgcode-workspaces/.archive/`

2. 当前工作区线程归档目录
- `<workspace>/.msgcode/archived-threads/`

## 降级口径

- 工作区根目录不存在：命令报错
- archive root 不存在：返回空
- archived-threads 不存在：返回空
- root 下出现非目录项：warning
- archived thread markdown 坏掉：warning

## 兼容口径

归档线程标题和来源优先读 front matter：

- `title`
- `transport`

没有时再回退：

- 文件名
- `chatId` 前缀

这样不挡以后 `0343` 的标题合同。

## 验证

- `test/p7-tk0338-appliance-archive-contract.test.ts`
- `tsc --noEmit`
- `npm run docs:check`

