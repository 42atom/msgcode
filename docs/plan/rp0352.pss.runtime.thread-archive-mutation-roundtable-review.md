---
owner: codex
assignee: codex
reviewer: user
why: thread archive 写面刚落，需要轻审议确认它没有污染 /clear，也没长出 session archive
scope: 只审 thread archive/restore mutation；不扩到 workspace archive
risk: low
accept: 有一份简短共识，确认这刀仍然是移动文件，不是新系统
links:
  - /Users/admin/GitProjects/msgcode/issues/tk0351.pss.runtime.p1.thread-archive-mutation-slice.md
  - /Users/admin/GitProjects/msgcode/docs/plan/rp0351.pss.runtime.thread-archive-mutation-slice.md
---

# RP0352：thread archive / restore 圆桌审议

## 结论

这刀可以过。

## 共识

1. 没污染 `/clear`
- `/clear` 继续只负责开新线程
- archive 继续只负责藏历史

2. 没做 session archive
- 仍只移动 thread markdown
- 没把 jsonl 主链做重

3. 文件为真相
- archive / restore 仍然只是目录内文件移动
- UI 不需要直接操作文件

4. 下一步边界清楚
- 若继续，优先接 archive 页 restore 交互
- 不要先做批量归档
