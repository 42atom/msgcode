# Plan: Doc Protocol Post Migration Closure

## Problem

文件名迁移已完成，协议收口也已落地。当前状态：

1. 入口层：已删除退役目录
2. 链接层：活跃文档中的旧路径引用已清理
3. 校验层：`docs:check` 已增加退役目录禁入检查

## Decision

- 正式 plan 入口只认 `docs/plan/pl*.md`
- 正式 research 入口只认 `docs/plan/rs*.md`
- 退役目录已删除
- `docs:check` 禁止旧目录路径引用

## Workstreams

### 1. 正式入口收口 - DONE

- ✓ 更新 `issues/_template.md`
- ✓ 更新 `docs/README.md`
- ✓ 更新 `issues/README.md`
- ✓ 退役目录已删除

### 2. 精确路径回填

- 扫描全部 `plan_doc`
- 将 `docs/plan/plan-YYMMDD-*.md` 回填为真实 `docs/plan/plNNNN...md`
- 必要时补充映射表，但最终不保留模糊逻辑

### 3. 旧路径残留治理

- 第一层：正式入口与模板，必须清零
- 第二层：活跃 `issues/` 与 `docs/tasks/`，优先清零
- 第三层：历史 `docs/plan/` / `docs/archive/` 正文，按价值决定是否保留

### 4. 校验逻辑收紧

- 删除 `check-doc-sync.ts` 中迁移期 slug 匹配兜底
- 恢复“路径不存在即失败”的严格校验

## Evidence

- ✓ `issues/_template.md` 已更新为新协议格式
- ✓ 退役目录已删除
- ✓ `scripts/check-doc-sync.ts` 已增加退役目录禁入检查

## Acceptance

收尾完成后，用户看到的正式协议应满足：

- 入口只有一套
- 模板只有一套
- `plan_doc` 只有精确路径
- `docs:check` 不再依赖迁移期特判
