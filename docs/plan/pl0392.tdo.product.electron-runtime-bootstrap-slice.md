---
owner: user
assignee: codex
reviewer: user
why: 正式 UI 已经有 `src/ui` 骨架，但仓库里还没有 Electron runtime；需要一张薄计划把 main/preload/renderer 建起来，给后续 host bridge 提供承载面
scope: 只定义 Electron runtime bootstrap 最薄顺序与边界；不进入具体 bridge 业务
risk: medium
accept: 存在一张清楚的 bootstrap 计划，固定 `tk0392 -> tk0391` 顺序
links:
  - /Users/admin/GitProjects/msgcode/issues/tk0392.tdo.product.p1.electron-runtime-bootstrap-slice.md
  - /Users/admin/GitProjects/msgcode/issues/tk0391.tdo.product.p1.readonly-thread-surface-host-bridge-slice.md
  - /Users/admin/GitProjects/msgcode/docs/plan/rf0000.rvw.product.electron-runtime-principles.md
---

# PL0392：Electron Runtime Bootstrap Slice

## 结论

先补 Electron runtime bootstrap，再做 readonly host bridge。

顺序固定：

1. `tk0392`
2. `tk0391`

## 最小内容

1. `electron` 依赖
2. main 入口
3. preload 入口
4. renderer 宿主入口

## 禁止项

1. 不做业务桥
2. 不做多页壳
3. 不做新服务层
4. 不回写 `ui-protype`

