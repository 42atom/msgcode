---
owner: user
assignee: codex
reviewer: user
why: 桌面写桥需要和读桥一样有进程边界，避免 Electron main process 背重执行
scope: 只规划并推进 write bridge process isolation；不碰流式与中断
risk: high
accept: 写桥通过 CLI 子进程承载 agent 执行，主进程只保留快速路径
links:
  - /Users/admin/GitProjects/msgcode/issues/tk0405.pss.runtime.p0.desktop-write-bridge-process-isolation.md
  - /Users/admin/GitProjects/msgcode/issues/tk0403.pss.product.p0.desktop-writable-thread-surface-mainline.md
---

# PL0405：Desktop Write Bridge Process Isolation

## 最小切法

1. `sendThreadInput()` 继续快速写入 user turn
2. `main.ts` 用 CLI 子进程承载后续 agent 执行
3. 子进程只做 assistant-only writeback

## 顺序

1. 先补 CLI 写桥入口
2. 再让 Electron main 改走 detached spawn
3. 最后补最小验证
