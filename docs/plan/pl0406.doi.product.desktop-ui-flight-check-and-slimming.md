---
owner: user
assignee: codex
reviewer: user
why: 桌面正式 UI 已可用，但接真界面前需要一次薄规整，减少旧命名、重复逻辑与假结构误导
scope: 只推进 desktop ui flight check and slimming；不改合同，不扩功能
risk: medium
accept: 正式 UI 更接近 Sketch 真相与 left/middle/right 结构；规整后仍保持当前主链行为
links:
  - /Users/admin/GitProjects/msgcode/issues/tk0406.doi.product.p1.desktop-ui-flight-check-and-slimming.md
---

# PL0406：Desktop UI Flight Check And Slimming

## 最小切法

1. 先收小去重
2. 再收命名
3. 再收 `renderer/main/ui shell` 的职责边界

## 当前顺序

1. 去掉 `renderer` 与 `ui shell` 的重复 `escapeHtml`
2. 再看 `readonly` 残留命名是否需要收口
3. 最后判断 `main.ts` 是否还需要更薄的桥边界

一句话：

先削噪音，再谈更深的规整。
