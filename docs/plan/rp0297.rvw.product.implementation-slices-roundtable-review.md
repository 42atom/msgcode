---
owner: codex
assignee: codex
reviewer: user
why: 在进入 0294/0296 实现前，对 0293/0294/0295/0296 做一次多视角收口，避免切片定义留坑
scope: 只审当前四张实施前切片的边界、缺口和优先级；不新增实现
risk: low
accept: 产出单一建议，说明先做什么、修什么、明确不做什么
links:
  - /Users/admin/GitProjects/msgcode/issues/tk0293.rvw.product.p1.main-ui-thread-surface-data-contract.md
  - /Users/admin/GitProjects/msgcode/issues/tk0294.rvw.runtime.p1.status-log-append-and-tail-read-contract.md
  - /Users/admin/GitProjects/msgcode/issues/tk0295.tdo.product.p1.wpkg-pack-registry-and-appliance-packs-surface.md
  - /Users/admin/GitProjects/msgcode/issues/tk0296.rvw.product.p1.workspace-people-truth-source-contract.md
---

# RP0297：实施前切片圆桌审阅

Decision:
- Recommendation: go
- Confidence: high
- Scope: 只推进 `0294` 和 `0296` 的最小实现；`0295` 继续停在注册表层，`0291` 不进入当前主线

Why:
- `0294` 直接补右栏“工作状况”真相源，是当前主界面最硬的缺口
- `0296` 直接补 people 页面和人物入口的真相源，能去掉原型假数据
- `0295` 不是主界面主链阻塞项，现在 hall.packs 为空不影响线程/聊天主面成立

Risks:
1. `0294` 若没有单一 writer，会退回到模型直写文件或多处各写一份
2. `0296` 若没有固定关联键，待关联身份会和人物表各写各的
3. `0295` 若同时开 `appliance packs` 和 `hall.packs` 两套出口，会重新长出重复消费面

Guardrails:
- 前置条件: `0294` 固定 `appendWorkspaceStatus(...)`、固定 `refPath + refLine`、固定 tail 10 条
- 前置条件: `0296` 固定 `channel + channelId` 为唯一关联键，`people-pending.json` 只读
- 监控指标: `docs:check` 通过，主界面原型不再显示无真相源的数字和未安装概念包
- 回滚条件: 若实现逼迫 UI 自建状态层，立即退回到文件真相源最小读取方案

Next Actions:
1. 先实现 `0294` 的单一 writer 和 tail reader
2. 再实现 `0296` 的 `people.json / people-pending.json` 最小读写
3. `0295` 只保留 `hall.packs` 单一路径，待主界面主链稳定后再补

Evidence:
- Docs: `pl0293.rvw`、`pl0294.rvw`、`pl0295.tdo`、`pl0296.rvw`
- Code: `/Users/admin/GitProjects/msgcode/src/cli/appliance.ts`
- UI: `/Users/admin/GitProjects/msgcode/ui-protype/main.html`、`/Users/admin/GitProjects/msgcode/ui-protype/people.html`
- Reports: `rp0293.rvw`、`rp0294.rvw`
