---
owner: user
assignee: codex
reviewer: user
why: logger 循环是当前最小、最确定的真实循环依赖，适合先拆
scope: 只拆 logger 共享类型文件并保持 index 出口不漂移
risk: low
accept: 循环消失，导出面不漂移，tsc 与 codegraph 验证通过
links:
  - /Users/admin/GitProjects/msgcode/issues/tk0385.rvw.runtime.p1.logger-shared-types-cycle-break.md
  - /Users/admin/GitProjects/msgcode/src/logger/index.ts
  - /Users/admin/GitProjects/msgcode/src/logger/console-transport.ts
  - /Users/admin/GitProjects/msgcode/src/logger/file-transport.ts
---

# PL0385：Logger Shared Types Cycle Break

## 结论

先拆 `logger`，不碰别的循环。

## 实现顺序

1. 新建 `src/logger/types.ts`
2. 把共享类型迁过去
3. `index.ts` 回导出这些类型
4. transport 改为直接 import `types.ts`
5. 用 `tsc` 和 `codegraph` 复验

## 不做什么

- 不改 logger 行为
- 不扩成新的 logger 架构层
- 不顺手收其他循环
