# Plan: Feishu Inbound Temp File Cleanup

Issue: 0197

## Problem

飞书非文本消息资源当前先下载到 `/tmp/msgcode-feishu-inbound`，再复制到 workspace `downloads/*`。  
主链已经打通，但临时文件没有回收，属于典型的小尾巴垃圾积累问题。

## Decision

只做最小修复：

1. 保持现有下载 -> 复制 -> 主链处理顺序不变
2. 在附件成功复制到 workspace 后，删除临时文件
3. 若复制失败或处理失败，只保留最小诊断事实，不新增清理服务或恢复层

## Workstreams

### 1. 生命周期确认

- 下载路径：`/tmp/msgcode-feishu-inbound`
- 正式落点：`<workspace>/downloads/*`
- 真正真相源是 workspace 内文件，不是临时下载文件

### 2. 删除时机

- `copyToVault()` 成功且返回 `localPath` 后即可删除临时文件
- 删除失败只记日志，不反向打断主链

### 3. 验收

- 不再残留成功处理过的临时文件
- 定向测试覆盖成功清理场景
- `tsc` 与 `docs:check` 继续通过

## Acceptance

- 这是 housekeeping，不是功能重构
- 以“清垃圾但不长新层”为唯一目标
