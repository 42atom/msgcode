---
owner: user
assignee: codex
reviewer: user
why: 记录 0326 这刀只实现 core settings 聚合读，不让 `doctor` 与 mutation 混进来
scope: 只记录 `appliance settings` 代码切片与回归锁；不展开 UI 实现
risk: low
accept: 留下稳定代码证据与测试证据，证明 `appliance settings` 还是薄 fan-in
links:
  - /Users/admin/GitProjects/msgcode/issues/tk0326.pss.runtime.p1.appliance-core-settings-read-slice.md
  - /Users/admin/GitProjects/msgcode/src/cli/appliance.ts
  - /Users/admin/GitProjects/msgcode/test/p7-tk0323-appliance-settings-contract.test.ts
---

# RP0326：appliance core settings 聚合读切片

## 结果

新增：

- `msgcode appliance settings --workspace <...> --json`

位置：

- `/Users/admin/GitProjects/msgcode/src/cli/appliance.ts`

## 这刀做了什么

1. 只聚合三段：
   - profile
   - general
   - capabilities
2. 每段保留：
   - `status`
   - `warnings`
   - `errors`
   - `data`
3. 顶层只做最小状态归约
4. `doctor` 保持独立，不并入 settings

## 这刀没做什么

- 没做 mutation
- 没 shell-out 调现有子命令
- 没加缓存
- 没做 settings 平台

## 验证

- `PATH="$HOME/.bun/bin:$PATH" bun test test/p7-tk0323-appliance-settings-contract.test.ts`
- `./node_modules/.bin/tsc --noEmit`
- `npm run docs:check`
