---
owner: codex
assignee: codex
reviewer: user
why: archive 写面刚落，需做一次轻审议，防止顺手长出索引表、兼容层或重命名策略
scope: 只审 workspace archive/restore mutation；不扩到 thread archive
risk: low
accept: 有一份简短审议结果，确认这刀仍然保持目录移动、冲突失败、无第二状态层
links:
  - /Users/admin/GitProjects/msgcode/issues/tk0349.rvw.runtime.p1.workspace-archive-mutation-slice.md
  - /Users/admin/GitProjects/msgcode/docs/plan/rp0349.rvw.runtime.workspace-archive-mutation-slice.md
---

# RP0350：workspace archive / restore 圆桌审议

## 结论

这刀可以过。

## 共识

1. 保持了 Unix 主链
- archive / restore 就是目录移动
- 没补数据库
- 没补 archive 索引表

2. fail-closed 是对的
- 同名冲突直接失败
- 不偷偷改名
- 不搞 reconcile

3. UI 角色收住了
- UI 只读结果
- 真正搬目录的是正式 CLI

4. 下一刀边界清楚
- 若继续，只做 thread archive / restore
- 不把 workspace archive 做成控制中心

