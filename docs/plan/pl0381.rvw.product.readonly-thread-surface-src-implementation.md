---
owner: user
assignee: codex
reviewer: user
why: `tk0381` 需要一张短 plan，把 src 第一刀的结构、入口和边界钉死
scope: 只服务 `tk0381`；只写 src/UI 第一刀的顺序和验收；不新增架构层
risk: low
accept: 存在一张很短的计划，能把三栏骨架、settings affordance、bridge 占位和非范围说清楚
implicit:
  waiting_for: ""
  next_check: ""
  stale_since: ""
links:
  - /Users/admin/GitProjects/msgcode/issues/tk0381.rvw.product.p1.readonly-thread-surface-src-implementation.md
  - /Users/admin/GitProjects/msgcode/issues/tk0377.tdo.product.p1.desktop-main-window-readonly-thread-surface-mainline.md
  - /Users/admin/GitProjects/msgcode/issues/tk0378.pss.product.p1.readonly-thread-surface-implementation.md
  - /Users/admin/GitProjects/msgcode/docs/plan/rf0001.rvw.product.electron-current-ui-constraints.md
---

# PL0381：Readonly Thread Surface Src Implementation

## 结论

第一刀只做一件事：

1. 在 `src/ui/main-window` 落主窗口只读线程面的正式结构骨架

## 最小顺序

### 1. 建结构

先落一个正式 UI 模块，能表达：

- 左栏 `workspace-tree`
- 中间 `thread`
- 右栏 `thread-rail`
- `settings` 一级入口 affordance
- `window.msgcodeReadonlySurface.runCommand` 的桥接占位

### 2. 建验证

补一个最小测试，锁住：

- 三栏骨架
- 只读状态键
- settings 入口
- 不出现 archive / new chat / composer / send

## 边界

- 不改 `ui-protype`
- 不做发消息
- 不做 settings 内容
- 不做 archive 内容
- 不做 Electron host bridge 实现
- 不做样式重做

## 验收

1. `src` 里能看到正式 UI 第一刀的真实结构。
2. `ui-protype` 仍然只是原型研究面。
3. 新测试能锁住只读主窗口的最薄骨架。
