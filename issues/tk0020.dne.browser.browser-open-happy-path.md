---
id: 0020
title: browser 打开网页 happy path 收口
status: done
owner: agent
labels: [bug, refactor, browser, tooling]
risk: medium
scope: browser runner/manifest/prompt
plan_doc: docs/plan/pl0020.dne.browser.browser-open-happy-path.md
links:
  - /Users/admin/.config/msgcode/log/msgcode.log
created: 2026-03-07
due:
---

## Context

- 最新真实日志显示，模型调用 `browser` 时直接走了 `tabs.open`，但没有先提供 `instanceId`，导致桥接层以 `TOOL_BAD_ARGS` 失败。
- 当前 PinchTab 底座已正常预启动，问题不在服务可用性，而在浏览器工具 happy path 太生硬。
- 后续日志继续证明，自动 launch 生效后又暴露出第二层桥接问题：模型传入的 `profileId=work-default` 并不是 PinchTab 真实 profile，导致 `profile not found`。

## Goal / Non-Goals

### Goals

- 让“打开网页”类请求可以通过一次 `browser tabs.open + url` 直接成功。
- 缺失 `instanceId` 时由桥接层自动补 `instances.launch`。
- 将可复用的 `instanceId` 返回给模型，便于后续 `tabs.snapshot/text/action`。

### Non-Goals

- 本轮不切换浏览器底座。
- 本轮不引入多实例调度策略。
- 本轮不约束模型回答内容。

## Plan

- [x] 创建并评审 Plan 文档：`docs/plan/plan-260307-browser-open-happy-path.md`
- [x] 收口 `tabs.open` happy path
- [x] 更新 browser 工具说明和提示
- [x] 增加并跑通回归测试

## Acceptance Criteria

1. `browser` 调用 `tabs.open` 且只给 `url` 时，不再报 `instanceId` 缺失。
2. 自动补实例后，`tabs.open` 返回结果中包含 `instanceId`。
3. 定向 runner/tool-loop 测试通过。
4. 若传入的 `profileId` 在 PinchTab 中不存在，桥接层会忽略它并退回默认 launch。

## Notes

- Logs: `/Users/admin/.config/msgcode/log/msgcode.log`
- 关键片段：
  - `Tool Bus: FAILURE browser`
  - `错误：browser: 'tabs.open' requires 'instanceId'`
  - `错误：BROWSER_HTTP_ERROR: profile "work-default" not found`
- Code:
  - `/Users/admin/GitProjects/msgcode/src/runners/browser-pinchtab.ts`
  - `/Users/admin/GitProjects/msgcode/src/tools/manifest.ts`
  - `/Users/admin/GitProjects/msgcode/prompts/agents-prompt.md`
- Tests:
  - `PATH="$HOME/.bun/bin:$PATH" bun test test/p5-7-r7a-browser-runner.test.ts test/p5-7-r9-t2-skill-global-single-source.test.ts`
  - `13 pass, 0 fail`
  - `PATH="$HOME/.bun/bin:$PATH" bun test test/p5-7-r7a-browser-runner.test.ts`
  - `10 pass, 0 fail`

## Links

- Plan: `docs/plan/plan-260307-browser-open-happy-path.md`
