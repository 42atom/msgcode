---
id: 0243
title: Checkpoint And Dispatch Drift Bdd Lock
status: pass
owner: agent
assignee: codex
reviewer: user
labels: [runtime, bdd, checkpoint]
why: pl0206 明确要求 checkpoint 在 restart 后继续生效，dispatch 漂移也必须被锁成可测行为
scope: checkpoint durable 恢复、dispatch drift 识别、主锚点来源边界；不新增额外状态机
risk: medium
accept: checkpoint 恢复来源清楚，dispatch 漂移只记为 drift，不抢文件真相
implicit:
  waiting_for: ""
  next_check: ""
  stale_since: ""
links:
  - /Users/admin/GitProjects/msgcode/docs/plan/pl0206.pss.runtime.work-vitals-session-bdd-design.md
  - /Users/admin/GitProjects/msgcode/issues/tk0205.pss.runtime.p0.work-continuity-truth-and-recovery-foundation.md
---

## Context

- checkpoint 和 dispatch 是恢复辅助，不是第二状态系统。

## Goal / Non-Goals

- Goal:
  - 锁 checkpoint restart 后可读
  - 锁恢复指针来源
  - 锁 dispatch drift 只告警不代裁决
- Non-Goals:
  - 不新增调度层
  - 不让 dispatch 反写任务状态

## Plan

- [x] 锁 checkpoint 在 restart 后继续生效
- [x] 锁主任务/委派任务恢复指针来源
- [x] 锁 dispatch drift 记录但不抢真相

## Acceptance Criteria

1. restart 后 checkpoint 与恢复指针仍可读。
2. dispatch 可镜像 checkpoint，但不承载状态真相。
3. 状态冲突时以文件名状态槽为准。

## Notes

- 这张单锁辅助恢复边界，不新增调度中间层。

## Findings

- 现有 work continuity BDD 已锁住这张单的核心边界：
  - [p6-work-continuity-foundation.test.ts](/Users/admin/GitProjects/msgcode/test/p6-work-continuity-foundation.test.ts)
- 已覆盖：
  - `A2` checkpoint restart 后仍可作为恢复锚点
  - `A3` dispatch drift 只记录不代裁决
  - `A3c` 多条 dispatch 并存时取最新 checkpoint

## Verification

- `npm run test:bun -- test/p6-work-continuity-foundation.test.ts test/p7-pl0210-wake-consume.test.ts test/p7-tk0239-work-capsule-source-stamp.test.ts`
- `./node_modules/.bin/tsc --noEmit`
- `npm run docs:check`

结果：

- `16 pass / 0 fail`
- `tsc` 通过
- `docs:check` 通过

## Links

- Plan: `docs/plan/pl0206.pss.runtime.work-vitals-session-bdd-design.md`
- Related: `issues/tk0205.pss.runtime.p0.work-continuity-truth-and-recovery-foundation.md`
