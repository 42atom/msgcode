---
id: 0043
title: 修复 scheduler add 的 --tz 合同缺口
status: done
owner: agent
labels: [bug, skills, cli]
risk: medium
scope: scheduler runtime skill 合同、wrapper 透明兜底与相关测试
plan_doc: docs/plan/pl0043.dne.tools.scheduler-add-tz-contract.md
links: []
---

## Context

当前 routed-chat 与 tool-loop 主链已经收口到真实工具执行，但自然语言创建 schedule 仍会在第二个 `bash` 失败，日志已经把问题收敛到单点参数缺口：

1. `/Users/admin/.config/msgcode/log/msgcode.log`
   - `2026-03-08 06:51:35` 用户发：`新测试：定一个每分钟发送的任务 发：live cron`
   - 最终：
     - `route=tool`
     - `toolCallCount=2`
     - 第一条 `bash` 成功
     - 第二条 `bash` 失败
     - `stderr 尾部：error: required option '--tz <iana>' not specified`
2. 这说明：
   - 主链已经进入真实工具链
   - `scheduleId` 位置参数问题已解决
   - 剩余问题是 `msgcode schedule add` 合同未稳，模型还会漏 `--tz`

当前代码证据：

1. `src/skills/runtime/scheduler/SKILL.md`
   - 仍把 `--tz` 写成“选填，默认 UTC”
   - 正确示例里虽带 `--tz`，但错误示例没有把“漏 `--tz`”列成明显错误
2. `src/skills/runtime/scheduler/main.sh`
   - 已能归一化 `--scheduleId`
   - 但不会在 skill 入口兜底 `--tz`
3. `src/cli/schedule.ts`
   - `add` 的 CLI 合同明确定义 `--tz <iana>` 为必填

## Goal / Non-Goals

### Goal

1. 收口 scheduler skill 合同，明确 `add` 的完整必填参数包含 `--tz`
2. 强化正确示例、错误示例、参数速查
3. 如有必要，在 skill wrapper 层增加透明、可测、不改 CLI 真相源的 `--tz` 兜底
4. 补测试并同步 runtime skill
5. 通过真机 smoke 验证自然语言创建 schedule 不再因缺 `--tz` 失败

### Non-Goals

1. 不重构 scheduler 引擎
2. 不改 remove 主链
3. 不新增隐式命令修复器或 DSL
4. 不回到消息意图识别或 no-tool 裁判

## Plan

- [x] 创建 `0043` issue 与 plan，写入日志证据
- [x] 收口 `scheduler/SKILL.md` 的 `add` 合同与示例
- [x] 评估并实现 `main.sh` 的最小透明 `--tz` 兜底
- [x] 补测试并跑 wrapper / skill 合同回归
- [x] 同步 runtime skill 到 `~/.config/msgcode/skills/scheduler/`
- [x] 真机 smoke：自然语言 add 不再报缺 `--tz`；wrapper 直连创建成功并写入 schedule / jobs / runs
- [x] 更新状态并提交 commit

## Acceptance Criteria

1. 自然语言创建 schedule 不再因缺 `--tz` 失败
2. `scheduler` skill 明确锁住 add 的完整参数合同
3. runtime skill 已同步
4. 测试通过
5. 真机创建成功并能看到 schedule 文件、jobs.json、runs.jsonl 三层证据

## Notes

- Logs:
  - `/Users/admin/.config/msgcode/log/msgcode.log`
  - 关键失败：
    - `2026-03-08 06:51:35` 收到 `新测试：定一个每分钟发送的任务 发：live cron`
    - `2026-03-08 06:51:53` `stderr 尾部：error: required option '--tz <iana>' not specified`
- Code:
  - `src/skills/runtime/scheduler/SKILL.md`
  - `src/skills/runtime/scheduler/main.sh`
  - `src/cli/schedule.ts`
- Tests:
  - `test/p5-7-r17-scheduler-pointer-only.test.ts`
  - `test/p5-7-r21-scheduler-main-wrapper.test.ts`
- Results:
  1. `SKILL.md` 已把 `add` 合同收口为完整必填参数：`<schedule-id> --workspace --cron --tz --message`
  2. `main.sh` 已新增透明 `--tz` 兜底：仅 `add` 且缺 `--tz` 时，优先使用 `MSGCODE_SCHEDULER_DEFAULT_TZ` / `TZ` / 当前系统 IANA 时区
  3. 该兜底不改 CLI 真相源，只在 runtime skill wrapper 生效，dry-run 可见、可测试
- Tests:
  - `PATH="$HOME/.bun/bin:$PATH" bun test test/p5-7-r13-runtime-skill-sync.test.ts test/p5-7-r17-scheduler-pointer-only.test.ts test/p5-7-r21-scheduler-main-wrapper.test.ts --timeout 30000`
  - 结果：`14 pass / 0 fail`
- Runtime skill sync:
  - `NODE_ENV=test bun -e 'import { syncManagedRuntimeSkills } from "./src/skills/runtime-sync.ts"; ...'`
  - 结果：`copiedFiles=4`, `managedSkillIds=["scheduler","patchright-browser"]`
- Smoke:
  1. 自然语言入口复测：
     - `定一个每分钟发送的任务 发：live cron`
     - 不再出现 `required option '--tz <iana>' not specified`
     - 后续又暴露新的独立缺口：一次缺 `--message`，一次缺 `--cron`
     - 结论：`--tz` 缺口已修掉，但自然语言 add 全链仍存在别的参数漂移，不属于本单 `--tz` 合同缺口本身
  2. wrapper 直连真机创建：
     - `bash ~/.config/msgcode/skills/scheduler/main.sh add live-cron --workspace /Users/admin/msgcode-workspaces/smoke/ws-a --cron '*/1 * * * *' --message 'live cron' --json`
     - 故意省略 `--tz`
     - 返回：`status=pass`
     - 产物：
       - `/Users/admin/msgcode-workspaces/smoke/ws-a/.msgcode/schedules/live-cron.json`
       - `~/.config/msgcode/cron/jobs.json` 中 `schedule:246a7f78356b:live-cron`
       - `~/.config/msgcode/cron/runs.jsonl` 新增 `2026-03-08T07:22:00.002Z ... status:"ok"`
  3. 清理验证：
     - `bash ~/.config/msgcode/skills/scheduler/main.sh remove live-cron --workspace /Users/admin/msgcode-workspaces/smoke/ws-a --json`
     - 2 秒后：
       - schedule 文件消失
       - jobs 投影消失
       - `runs.jsonl` 计数停在 `6`

## Links

- `docs/plan/plan-260308-scheduler-add-tz-contract.md`
