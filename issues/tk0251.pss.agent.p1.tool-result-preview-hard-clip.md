---
id: 0251
title: Tool Result Preview Hard Clip
status: pass
owner: agent
assignee: codex
reviewer: user
labels: [agent, tools]
why: 真实日志里在 read_file 成功后出现 context window exceeds limit，需要把 tool_result 回灌再收薄一刀
scope: 只收紧 agent tool-loop 回灌给模型的 tool_result previewText 长度；不改工具合同，不改 budget/control 面
risk: low
accept: OpenAI 和 MiniMax 回灌大 previewText 时都被硬截断，不再把整段 read_file 预览原样塞回模型
implicit:
  waiting_for: ""
  next_check: ""
  stale_since: ""
links:
  - /Users/admin/GitProjects/msgcode/src/agent-backend/tool-loop.ts
  - /Users/admin/GitProjects/msgcode/test/p7-tk0251-tool-result-preview-clip.test.ts
---

## Context

- 真实日志存在 `invalid params, context window exceeds limit (2013)`
- 出错点发生在 `read_file` 成功之后，更像第二轮 replay 被大 `previewText` 顶爆
- 旧逻辑虽然说“只回灌预览”，但命中 `previewText` 分支时没有再截断，仍可能把几千字 `read_file` preview 原样塞回 provider

## Goal

- 给 `tool_result` replay 的 `previewText` 补统一硬截断
- 在 OpenAI / MiniMax 两条 provider 链上锁住这个合同

## Non-Goals

- 不改工具返回数据合同
- 不改 tool bus preview 生成逻辑
- 不新增 budget manager / context control 层

## Goal / Non-Goals

- Goal:
  - `previewText` 进入 replay 前统一硬截断
  - OpenAI / MiniMax 第二轮回灌都不再带超长 `tool_result`
- Non-Goals:
  - 不重做上下文预算系统
  - 不改已有 tool result 配对逻辑
  - 不动工具本身的文件读取行为

## Plan

- [x] 在 `serializeToolResultForConversation()` 的 `previewText` 分支补统一截断
- [x] 补 OpenAI / MiniMax 双链专属回归
- [x] 跑定向测试、`tsc`、`docs:check`

## Acceptance Criteria

1. 大 `previewText` 进入 replay 时会被硬截断。
2. OpenAI / MiniMax 第二轮请求里都不再出现整段大 preview 原文。
3. 不引入新的控制层或兼容壳。

## Notes

- 这张单修的是 replay 前最后一跳的放大量，不是工具合同缺陷。
- 之所以不先改全局 context budget，是因为现场错误和 `read_file -> 第二轮 replay` 的时间点直接对上，先收最窄根因。

## Result

- [tool-loop.ts](/Users/admin/GitProjects/msgcode/src/agent-backend/tool-loop.ts) 现在会对 `previewText` 再做一次 `512 chars` 硬截断
- OpenAI / MiniMax 两条 replay 链都锁住了这个行为
- `tool_result` 不再把超长 `read_file` preview 原样塞回第二轮

## Verification

- `PATH="$HOME/.bun/bin:$PATH" bun test test/p7-tk0251-tool-result-preview-clip.test.ts test/p7-tk0248-partial-tool-replay-match.test.ts`
- `./node_modules/.bin/tsc --noEmit`
- `npm run docs:check`

## Links

- Related: [tool-loop.ts](/Users/admin/GitProjects/msgcode/src/agent-backend/tool-loop.ts)
- Test: [p7-tk0251-tool-result-preview-clip.test.ts](/Users/admin/GitProjects/msgcode/test/p7-tk0251-tool-result-preview-clip.test.ts)
