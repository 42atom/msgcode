---
id: 0404
title: Sketch UI Truth Source Mainline
status: pss
owner: user
assignee: codex
reviewer: user
labels: [product, ui, sketch]
why: 正式 UI 已开始落地，Sketch 已成为视觉与结构真相源；需要明确它与正式 UI、token、组件同步的主线合同
scope: 只定义 Sketch 作为正式 UI 真相源的边界、同步口径与非范围；不直接扩设计系统，不重做页面
risk: medium
accept: 仓库内存在一张明确单子，能回答哪份 Sketch 是 UI 真相源、哪些内容必须同步、哪些只是参考
implicit:
  waiting_for: ""
  next_check: ""
  stale_since: ""
links:
  - /Users/admin/GitProjects/msgcode/AIDOCS/ui-design-src/ui-protype-v1.sketch
  - /Users/admin/GitProjects/msgcode/src/ui/main-window/readonly-thread-surface.ts
---

# 任务：Sketch UI Truth Source Mainline

## Context

正式 UI 的视觉与结构真相，指向：

- `/Users/admin/GitProjects/msgcode/AIDOCS/ui-design-src/ui-protype-v1.sketch`

当前代码真相在：

- `/Users/admin/GitProjects/msgcode/src/ui/main-window/readonly-thread-surface.ts`

若不先钉死真相源，正式 UI 与 Sketch 会继续漂。

## Goal / Non-Goals

### Goal

1. 明确哪份 Sketch 是正式 UI 真相源
2. 明确正式 UI 必须同步什么
3. 明确哪些只是参考

### Non-Goals

1. 不把 Sketch 当运行时配置中心
2. 不把所有草稿页都当真相
3. 不要求正式 UI 一次性 1:1 覆盖所有原型细节

## Plan

1. 先同步 token
2. 再同步高复用组件骨架
3. 再同步页面一级结构
4. 最后才收状态细节

## Acceptance Criteria

1. 仓库内明确指向 `ui-protype-v1.sketch`
2. 文档明确 token、组件、结构是必须同步项
3. 文档明确非范围，避免把 Sketch 变成运行时控制面

## Notes

### 必须同步

1. 全局 token
   - 颜色
   - 字体
   - 文字层级
2. 一级结构
   - 左中右分区
   - header / stage / dock / rail
3. 高复用组件
   - sidebar item
   - chat message
   - button / switch
   - observer row

## 当前 pass 口径

- Sketch 真相源已明确指向 `ui-protype-v1.sketch`
- token 与高频组件骨架已开始回灌正式 UI
- 这张单现在作为持续对齐合同保留，不再是待做事项

## Links

- /Users/admin/GitProjects/msgcode/AIDOCS/ui-design-src/ui-protype-v1.sketch
- /Users/admin/GitProjects/msgcode/src/ui/main-window/readonly-thread-surface.ts

一句话：

Sketch 负责视觉与结构真相；正式 UI 负责可运行实现；两者通过 token 与组件骨架持续对齐。
