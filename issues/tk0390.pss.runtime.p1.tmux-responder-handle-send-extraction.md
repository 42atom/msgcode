---
id: 0390
title: Tmux Responder Handle Send Extraction
status: pss
labels: [runtime, tmux, complexity]
owner: user
assignee: codex
reviewer: user
why: `src/tmux/responder.ts` 中 `handleTmuxSend` 认知复杂度过高；在循环依赖收口后，需要把它抽成更薄的主骨架，降低后续修改爆炸半径
scope: 只对 `handleTmuxSend` 做纯提取式瘦身；不改外部行为，不引入新控制层
risk: medium
accept: `handleTmuxSend` 主函数明显变薄，复杂分支被提取为具名纯函数或窄 helper，行为与测试不回退
implicit:
  waiting_for: ""
  next_check: ""
  stale_since: ""
links:
  - /Users/admin/GitProjects/msgcode/src/tmux/responder.ts
---

# TK0390：Tmux Responder Handle Send Extraction

## Context

`codegraph` 复杂度显示：

- `handleTmuxSend`
- 认知复杂度 `207`
- cyclomatic `101`

这已经不是“长一点”，而是后续修改高风险区。

## Goal / Non-Goals

### Goal

- 保留现有行为
- 只把复杂分支和字符串/状态拼接逻辑提成具名 helper
- 让主函数留下决策骨架

### Non-Goals

- 不重写 tmux responder 语义
- 不引入新的 responder 编排层
- 不顺手处理别的高复杂度文件

## Acceptance Criteria

1. `handleTmuxSend` 主体明显缩短
2. 提取出来的 helper 职责单一、命名清楚
3. 相关 tmux 最小测试、`tsc --noEmit` 通过

## Plan

1. 先按分支簇切块
2. 再提纯字符串处理/状态判断/输出组装
3. 最后只保留主骨架在 `handleTmuxSend`

## Notes

- 这刀排在循环依赖之后，避免边断依赖边抽大函数
- 已抽出读取模式初始化、基线采样、发送提交、单轮读取 helper；主函数只保留会话检查、阶段串联、轮询骨架与超时收尾

## Links

- /Users/admin/GitProjects/msgcode/docs/plan/pl0387.pss.runtime.structural-refactor-repair-mini-baseline.md
