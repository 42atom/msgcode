---
id: 0195
title: Tasks Directory Archive Physical Closure
status: pass
owner: agent
assignee: codex
reviewer: user
labels: [docs, archive, cleanup]
risk: low
scope: docs/tasks 历史任务单迁入 docs/archive，docs/tasks 只保留现役说明入口
plan_doc: docs/plan/pl0195.tdo.docs.tasks-directory-archive-physical-closure.md
links:
  - issues/tk0193.dne.docs.doc-surface-polish-and-trash-cleanup.md
  - issues/tk0194.dne.docs.historical-backlink-cleanup-and-archive-alignment.md
---

## Context

- `docs/tasks/README.md` 已明确声明本目录归档，不再接收新任务。
- 但大量老任务单仍物理留在 `docs/tasks/`，会继续制造“现役目录里还有老命名任务单”的错觉。
- 这不是命名协议问题，而是目录职责还没彻底收口。

## Goal / Non-Goals

- Goal:
  - 把 `docs/tasks/*.md` 的历史任务单整批迁入 `docs/archive/`
  - `docs/tasks/` 最终只保留 `README.md`
  - 更新关键入口与必要回链，避免误导
- Non-Goals:
  - 不重写历史正文
  - 不重命名单个历史任务单
  - 不重开文档协议迁移

## Plan

- [x] 盘点 `docs/tasks/` 历史文件与目标归档目录
- [x] 批量 `git mv` 到 `docs/archive/tasks/` 或等价归档目录
- [x] 更新 `README` / 入口文档 / 必要回链
- [x] 跑 `npm run docs:check`

## Acceptance Criteria

1. `docs/tasks/` 只保留 `README.md`。
2. 历史任务单已进入明确归档目录。
3. 入口文档不再把 `docs/tasks/` 表述成现役任务目录。
4. `npm run docs:check` 通过。

## Notes

- 这是目录职责收口，不是再做一轮命名改造。
- 默认优先 `docs/archive/tasks/`，除非执行时发现更准确的归档目录名更合适。

## Findings

- `docs/tasks/` 物理上已经只剩：
  - [README.md](/Users/admin/GitProjects/msgcode/docs/tasks/README.md)
- 历史任务单已进入明确归档目录：
  - [docs/archive/tasks](/Users/admin/GitProjects/msgcode/docs/archive/tasks)
  - 当前文件数：`128`
- 入口口径已对齐：
  - [docs/README.md](/Users/admin/GitProjects/msgcode/docs/README.md) 明确把 `docs/tasks/` 写成归档目录
  - [rs9001.dne.docs.msgcode-architecture.md](/Users/admin/GitProjects/msgcode/docs/plan/rs9001.dne.docs.msgcode-architecture.md) 现在也只把 `docs/tasks/` 当历史归档，不再当现役任务目录
- 当前这张单不需要再做额外批量搬迁；物理收口已经先发生，剩下的是状态真相回写

## Verification

- `rg --files /Users/admin/GitProjects/msgcode/docs/tasks`
- `find /Users/admin/GitProjects/msgcode/docs/archive/tasks -maxdepth 1 -type f | wc -l`
- `npm run docs:check`

结果：

- `docs/tasks/` 仅剩 `README.md`
- `docs/archive/tasks/` 下有 `128` 个历史任务单文件
- `docs:check` 通过

## Links

- Plan: `docs/plan/pl0195.tdo.docs.tasks-directory-archive-physical-closure.md`
- Previous cleanup: `issues/tk0193.dne.docs.doc-surface-polish-and-trash-cleanup.md`
