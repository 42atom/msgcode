---
id: 0196
title: AIDOCS Structure Closure And Archive Cleanup
status: pass
owner: agent
assignee: codex
reviewer: user
labels: [docs, cleanup, archive]
risk: low
scope: AIDOCS 分层收口、重复目录合并、历史资料降权归档、README 口径修正
plan_doc: docs/plan/pl0196.pss.docs.aidocs-structure-closure-and-archive-cleanup.md
links:
  - issues/tk0193.dne.docs.doc-surface-polish-and-trash-cleanup.md
  - issues/tk0195.pss.docs.p2.tasks-directory-archive-physical-closure.md
---

## Context

- 当前仓库正式真相源已经收口到 `issues/` + `docs/plan/`。
- `AIDOCS/` 仍是全仓最杂的区域，主要问题不是文件名，而是目录职责重叠：
  - `refs/` 与旧 `references/` 语义曾重复
  - `design/`、`notes/` 仍混着现役草稿和历史笔记
  - `imessage-compare/`、`p5-7-r3c-data/` 这类历史资料缺少统一归档位
  - `AIDOCS/README.md` 还保留不存在或过时的目录口径

## Goal / Non-Goals

- Goal:
  - 合并重复目录，收清 `AIDOCS` 目录职责
  - 让现役辅助材料、历史参考、一次性数据集分层清楚
  - 修正 `README`，避免继续误导新同学
- Non-Goals:
  - 不把 `AIDOCS` 升格为正式真相源
  - 不重做正式文档命名协议
  - 不重写大批历史正文内容

## Plan

- [x] 盘点 `AIDOCS` 现役 / 历史 / 冗余目录
- [x] 合并 `references/` → `refs/`
- [x] 处理 `imessage-compare/`、`p5-7-r3c-data/` 等历史资料归位
- [x] 为 `design/`、`notes/` 补清晰的“草稿/笔记”定位
- [x] 更新 `AIDOCS/README.md`
- [x] 跑 `npm run docs:check`

## Acceptance Criteria

1. `AIDOCS` 不再存在明显重复职责目录。
2. `README` 与实际目录一致。
3. 历史资料有明确归档位，不再像现役辅助输入。
4. `npm run docs:check` 通过。

## Notes

- 优先做目录与职责收口，文件名只在顺手且低风险时修。
- 原则：现役辅助材料保留；纯历史参考和一次性数据下沉归档。

## Findings

- 重复目录已经收口：
  - `AIDOCS/references/` 已不存在
  - [AIDOCS/refs](/Users/admin/GitProjects/msgcode/AIDOCS/refs) 作为唯一参考资料入口保留
- 历史资料已有明确归位：
  - [AIDOCS/reports/archive/historical/imessage-compare](/Users/admin/GitProjects/msgcode/AIDOCS/reports/archive/historical/imessage-compare)
  - [AIDOCS/reports/archive/historical/p5-7-r3c-data](/Users/admin/GitProjects/msgcode/AIDOCS/reports/archive/historical/p5-7-r3c-data)
- 草稿/笔记口径已明确：
  - [AIDOCS/README.md](/Users/admin/GitProjects/msgcode/AIDOCS/README.md) 明确把 `design/`、`notes/` 写成非正式真相源
- README 与真实目录已对齐：
  - 删掉了不存在的 `product/`
  - 补上了真实存在的 `artifacts/`、`audio/`、`media/`、`msgcode-2.x` 等目录职责

## Verification

- `find /Users/admin/GitProjects/msgcode/AIDOCS -maxdepth 1 -type d | sort`
- `test -d /Users/admin/GitProjects/msgcode/AIDOCS/references && echo REFERENCES_EXISTS || echo REFERENCES_MISSING`
- `find /Users/admin/GitProjects/msgcode/AIDOCS/reports/archive -maxdepth 2 -type d | sort`
- `npm run docs:check`

结果：

- `references/` 已不存在，`refs/` 保留
- 历史资料已在 `reports/archive/historical/`
- `AIDOCS/README.md` 与真实目录一致
- `docs:check` 通过

## Links

- Plan: `docs/plan/pl0196.pss.docs.aidocs-structure-closure-and-archive-cleanup.md`
