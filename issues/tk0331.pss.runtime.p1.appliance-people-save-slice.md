---
id: 0331
title: Appliance People Save Slice
status: pss
owner: user
assignee: codex
reviewer: user
labels: [runtime, ui, people]
why: 0330 已冻结第一版 people 写边界，需要一个最薄写口承接“存入通讯录/编辑人物”，避免 UI 直写 CSV
scope: 只实现 `appliance people-save`、CSV upsert 和黑盒回归锁；不处理 pending 过滤，不实现 tags，不实现全局人物中心
risk: medium
accept: CLI 能按 `channel + chatId + senderId` upsert CSV，保留 first_seen_at 与 role，测试通过
implicit:
  waiting_for: ""
  next_check: ""
  stale_since: ""
links:
  - /Users/admin/GitProjects/msgcode/issues/tk0330.pss.product.p1.workspace-people-mutation-baseline.md
  - /Users/admin/GitProjects/msgcode/docs/plan/pl0330.tdo.product.workspace-people-mutation-baseline.md
  - /Users/admin/GitProjects/msgcode/docs/plan/rp0331.rvw.runtime.appliance-people-save-slice.md
---

# 任务：People 最薄写口实现切片

## Context

People 读取主链已经成立，但 UI 原型里的：

- 存入通讯录
- 编辑人物

都还没有正式写入口。

如果不补这条线，后面前端一定会去碰 CSV。

## Goal / Non-Goals

### Goal

- 实现 `msgcode appliance people-save`
- 复用现有 `character-identity` CSV
- 固定为单文件 upsert

### Non-Goals

- 不过滤 pending
- 不实现 tags
- 不实现 role 编辑
- 不实现批量导入

## Plan

- [x] 新增最薄 CSV helper
- [x] 新增 `appliance people-save`
- [x] 补黑盒回归锁
- [x] 保住现有 `appliance people` 读面

## Acceptance Criteria

1. 缺 CSV 时能创建文件并写入表头。
2. 同一键更新时保留 `first_seen_at` 与 `role`。
3. 黑盒测试、类型检查、文档检查通过。

## Notes

- 这刀只补写口，不顺手处理 pending 过滤。
- `tags` 继续留在设计层，不进入当前真相源。

## Links

- Plan: [pl0330.tdo.product.workspace-people-mutation-baseline.md](/Users/admin/GitProjects/msgcode/docs/plan/pl0330.tdo.product.workspace-people-mutation-baseline.md)
- Report: [rp0331.rvw.runtime.appliance-people-save-slice.md](/Users/admin/GitProjects/msgcode/docs/plan/rp0331.rvw.runtime.appliance-people-save-slice.md)
