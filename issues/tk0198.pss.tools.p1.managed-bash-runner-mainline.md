---
id: 0198
title: Managed Bash Runner Mainline
status: pass
owner: agent
labels: [tools, runtime, refactor]
risk: medium
scope: bash runner、tool bus 结果、测试锁
plan_doc: docs/plan/pl0198.pss.tools.managed-bash-mainline.md
links:
  - issues/tk0199.pss.runtime.p1.managed-bash-bootstrap-contract.md
  - issues/tk0200.pss.agent.p1.shell-contract-doc-and-prompt-alignment.md
created: 2026-03-14
due:
---

## Context

- 当前 `bash` 工具对外名义是 Bash，但运行时仍通过 `shell: true` 执行。
- 在当前 macOS 环境里，这条链实际落到 `/bin/sh`，不是显式 Bash。
- 用户目标已经明确：一次性把工具装到位，减少模型后续自己试探 shell 差异。
- 因此第一刀应该先把运行时合同收口成“托管现代 Bash”，而不是继续放任 shell 漂移。

## Goal / Non-Goals

- Goal:
  - `bash` runner 显式执行 Homebrew Bash
  - 缺失时返回真实物理边界错误
  - 增加测试锁住 shell 路径与错误行为
- Non-Goals:
  - 不引入 `/bin/sh`、`zsh`、系统 `/bin/bash` 3.2 的自动 fallback
  - 不改工具名
  - 不在本单处理 bootstrap 文档与 prompt 文案

## Plan

- [x] 抽出托管 Bash 路径解析逻辑，只承认 Homebrew Bash 位置
- [x] `bash-runner` 改为 `shell: false + <bash> -lc <command>` 显式执行
- [x] 缺失托管 Bash 时返回可诊断错误，包含建议安装路径或命令
- [x] 增加回归测试：锁住执行路径、版本特征和缺失错误
- [x] 验证 Tool Bus 返回结构不回归

## Acceptance Criteria

1. [src/runners/bash-runner.ts](/Users/admin/GitProjects/msgcode/src/runners/bash-runner.ts) 不再使用 `shell: true`。
2. `bash` 工具只承认 `/opt/homebrew/bin/bash` 或 `/usr/local/bin/bash`。
3. 托管 Bash 缺失时，工具失败且错误可诊断，不发生 shell 自动回退。
4. 测试覆盖至少包含：
   - 执行器使用的 shell 路径
   - `BASH_VERSION` 为托管 Bash 返回
   - 缺失托管 Bash 的失败路径

## Notes

- 本单是主链单。只有它收口后，安装链和文档链才有统一依附对象。

## Findings

- `bash-runner` 已改成显式托管 Bash 主链：
  - [bash-runner.ts](/Users/admin/GitProjects/msgcode/src/runners/bash-runner.ts)
- 当前只认：
  - `/opt/homebrew/bin/bash`
  - `/usr/local/bin/bash`
- 运行时不再使用 `shell: true`，也不再吃用户 profile / rc 噪声
- 托管 Bash 缺失时，会直接返回真实缺依赖错误，不再偷偷回退到 `/bin/sh`
- 回归锁已补：
  - [p5-7-r3f-bash-runner.test.ts](/Users/admin/GitProjects/msgcode/test/p5-7-r3f-bash-runner.test.ts)
  - [p5-7-r3h-tool-failure-diagnostics.test.ts](/Users/admin/GitProjects/msgcode/test/p5-7-r3h-tool-failure-diagnostics.test.ts)
  - [p7-tk0198-managed-bash-mainline.test.ts](/Users/admin/GitProjects/msgcode/test/p7-tk0198-managed-bash-mainline.test.ts)

## Verification

- `npm run test:bun -- test/p5-7-r3f-bash-runner.test.ts test/p5-7-r3h-tool-failure-diagnostics.test.ts test/p7-tk0198-managed-bash-mainline.test.ts`
- `./node_modules/.bin/tsc --noEmit`

结果：

- `30 pass / 0 fail`
- `tsc` 通过

## Links

- Plan: `docs/plan/pl0198.pss.tools.managed-bash-mainline.md`
- Follow-up: `issues/tk0199.pss.runtime.p1.managed-bash-bootstrap-contract.md`
- Follow-up: `issues/tk0200.pss.agent.p1.shell-contract-doc-and-prompt-alignment.md`
