---
id: 0075
title: 补飞书群聊观测并正式清退 pinchtab-browser 残留
status: done
owner: agent
labels: [bug, refactor, docs]
risk: low
scope: feishu 入站日志观测、repo retired runtime skill 清退
plan_doc: docs/plan/pl0075.dne.browser.feishu-observability-and-pinchtab-retirement.md
links:
  - docs/plan/plan-260311-feishu-observability-and-pinchtab-retirement.md
  - src/feishu/transport.ts
  - src/skills/runtime-sync.ts
---

## Context

上一轮 review 已指出两处尾巴：

1. 飞书 transport 已经能判断 `isGroup/chat_type`，但日志没打印，私聊/群聊排障还要靠猜。
2. `pinchtab-browser` 已明确退役，但源码目录仍留在 `src/skills/runtime/`，形成第二真相源。

## Goal / Non-Goals

### Goal

- 在飞书入站日志里补齐 `chatType/isGroup` 观测。
- 让 `pinchtab-browser` 正式退出 repo runtime 目录，只保留历史文档引用。

### Non-Goals

- 不继续扩 skill 分层设计
- 不触碰 `zai-vision-mcp` 之外的其他 legacy-active skills
- 不新增消息平台或额外日志层

## Plan

- [x] 在 `src/feishu/transport.ts` 收口 `chat_type -> isGroup` 的纯函数，并将字段打进入站日志
- [x] 补最小回归锁，验证群聊判断标准化逻辑
- [x] 将 `src/skills/runtime/pinchtab-browser/` 移出 repo 主目录，保留本地 `.trash` 备份
- [x] 更新 changelog / notes，记录该 skill 已彻底退出 runtime 真相源

## Acceptance Criteria

1. 飞书入站日志包含 `chatType` 与 `isGroup`。
2. 对 `group/p2p/undefined` 的判断有测试锁。
3. `pinchtab-browser` 不再存在于 `src/skills/runtime/`。
4. 运行时同步和测试主链不回归。

## Notes

- 本轮是上一轮 review 剩余项的续做，不扩 scope。
- 2026-03-11 验证结果：
  - `PATH="$HOME/.bun/bin:$PATH" bun test test/p5-7-r13-feishu-inbound-observability.test.ts`
  - `PATH="$HOME/.bun/bin:$PATH" bun test test/p5-7-r13-runtime-skill-sync.test.ts`
  - 结果：`10 pass / 0 fail`
  - `src/skills/runtime/pinchtab-browser/` 已移至本地 `.trash/2026-03-11-pinchtab-browser/`

## Links

- [docs/plan/plan-260311-feishu-observability-and-pinchtab-retirement.md](/Users/admin/GitProjects/msgcode/docs/plan/plan-260311-feishu-observability-and-pinchtab-retirement.md)
- [src/feishu/transport.ts](/Users/admin/GitProjects/msgcode/src/feishu/transport.ts)
- [src/skills/runtime/pinchtab-browser/SKILL.md](/Users/admin/GitProjects/msgcode/src/skills/runtime/pinchtab-browser/SKILL.md)
