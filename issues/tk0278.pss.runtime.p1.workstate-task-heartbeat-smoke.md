---
id: 0278
title: workstate task heartbeat smoke
status: pss
owner: user
assignee: codex
reviewer: user
labels: [runtime, smoke, workstate]
why: 0259/0277 已完成协议与读回接线，还需要一条更接近真实 task runtime 的 smoke 证明 WORKSTATE 真能穿过 heartbeat 续跑
scope: 在 task runtime smoke 基座上补一条 /task run -> continuable -> heartbeat -> executeAgentTurn 的 WORKSTATE 样本；不接飞书真机
risk: medium
accept: 存在一条 task/heartbeat smoke，第一轮写下 WORKSTATE，第二轮 heartbeat 经真实 assembleAgentContext 和 executeAgentTurn 把 WORKSTATE 交给模型
implicit:
  waiting_for: ""
  next_check: ""
  stale_since: ""
links:
  - /Users/admin/GitProjects/msgcode/test/p7-tk0278-workstate-task-heartbeat-smoke.test.ts
  - /Users/admin/GitProjects/msgcode/issues/tk0277.pss.runtime.p1.workstate-read-injection-at-context-rebuild.md
  - /Users/admin/GitProjects/msgcode/docs/testing/feishu-live-smoke.md
---

# 任务：WORKSTATE task/heartbeat smoke

## Context

`0277` 已证明：

- `WORKSTATE` 能被读回
- `WORKSTATE` 能进入 `executeAgentTurn`

但还缺一条更接近真实 task runtime 的 smoke：

- `/task run`
- 第一轮续跑写下 `WORKSTATE`
- heartbeat 下一轮继续
- 第二轮通过真实 `assembleAgentContext -> executeAgentTurn`

## Goal / Non-Goals

### Goal

- 补一条 task runtime 的 WORKSTATE smoke 样本
- 证明 `WORKSTATE` 不是孤立单测，而是能穿过续跑主链

### Non-Goals

- 不接飞书真机
- 不引入 graph/router
- 不改 prompt 合同

## Plan

- [x] 基于现有 `p5-7-r12-t9` smoke 基座补新样本
- [x] 第一轮写下 `<workspace>/.msgcode/workstates/<taskId>.md`
- [x] 第二轮 heartbeat 走真实 `assembleAgentContext + executeAgentTurn`
- [x] 断言最终模型请求体顺序为 `WORKSTATE -> summary -> window -> 当前问题`

## Acceptance Criteria

1. 第一轮续跑后，`<workspace>/.msgcode/workstates/<taskId>.md` 已存在。
2. 第二轮 heartbeat 通过真实 `assembleAgentContext -> executeAgentTurn` 完成。
3. 最终模型请求体里 `WORKSTATE` 排在 `summary` 前。

## Verification

- `PATH="$HOME/.bun/bin:$PATH" bun test test/p7-tk0278-workstate-task-heartbeat-smoke.test.ts`
- `./node_modules/.bin/tsc --noEmit`
- `npm run docs:check`

## Notes

- 这条 smoke 的价值是把 `0276` 的文本试跑和 `0277` 的读回接线在 task runtime 主链里串起来。
- 当前已证明：第一轮写下 `WORKSTATE`，第二轮 heartbeat 会经真实主链把它交给模型。

## Links

- Test: /Users/admin/GitProjects/msgcode/test/p7-tk0278-workstate-task-heartbeat-smoke.test.ts
- Parent: /Users/admin/GitProjects/msgcode/issues/tk0277.pss.runtime.p1.workstate-read-injection-at-context-rebuild.md
