---
id: 0256
title: Schedule Unsupported Payload Kind Family Followup
status: tdo
owner: agent
assignee: codex
reviewer: user
labels: [runtime, schedule]
why: family 工作区的定时任务仍在真实日志里反复报“不支持的 payload.kind”，说明 schedule 历史载荷兼容还没收干净
scope: 只核 family 当前 schedule 文件里的真实 payload.kind 漂移，并补最薄读路径兼容或数据收口；不重做 schedule 架构
risk: medium
accept: family 当前失败的 schedule 触发不再因 payload.kind 被 runtime 直接 fail-closed，相关验证通过
implicit:
  waiting_for: ""
  next_check: ""
  stale_since: ""
links:
  - /Users/admin/.config/msgcode/log/msgcode.log
  - /Users/admin/msgcode-workspaces/family/.msgcode
---

## Context

- 最新日志仍有真实失败：
  - 2026-03-19 05:40:00 `send-kids-school`
  - 2026-03-19 03:45:00 `pick-up-kids`
- 失败口径一致：`error=不支持的 payload.kind`
- 这说明 `tk0247` 收掉的历史 `agentPrompt` 漂移还不是全部，family 当前 schedule 里还有另一类旧 payload 没被读路径兼容覆盖

## Goal

- 找出 family 当前 schedule 文件里的真实旧 payload.kind
- 只补最薄读路径兼容或最小数据收口
- 让现有 family schedule 恢复可运行

## Non-Goals

- 不重写 schedule v2
- 不新增 schedule 中间控制层
- 不把问题泛化成新的编排系统

## Goal / Non-Goals

- Goal:
  - 收掉 family 当前 schedule 的 `payload.kind` 漂移
- Non-Goals:
  - 不重构 schedule 主链
  - 不新增新的 payload 控制面

## Plan

- [ ] 读取 family 当前 `.msgcode/schedule*` 真相文件
- [ ] 确认失败 payload.kind 的真实值
- [ ] 选择最薄修法：读路径兼容或就地数据收口
- [ ] 补回归并验证

## Acceptance Criteria

1. 能明确指出 family 当前失败 schedule 的真实旧 payload.kind。
2. runtime 不再因为这类 payload.kind 直接失败。
3. 相关验证通过。

## Notes

- 这是当前最新日志里的真实故障，不是历史残影。
- 首选修法是薄兼容或就地数据收口；只有证明确实不够，才考虑更大改动。

## Links

- Log: [msgcode.log](/Users/admin/.config/msgcode/log/msgcode.log)
- Workspace: [family/.msgcode](/Users/admin/msgcode-workspaces/family/.msgcode)
