---
id: 0277
title: workstate read injection at context rebuild
status: rvw
owner: user
assignee: codex
reviewer: user
labels: [runtime, memory, workstate]
why: 0259 已经证明 WORKSTATE 对跨窗口恢复有增益，下一步需要验证最薄的读回接线
scope: 只在有 taskId 的 context rebuild 链路读取 WORKSTATE 并注入模型上下文；不扩到 message 链，不新增控制层
risk: medium
accept: task/heartbeat 续跑链能读取 <workspace>/.msgcode/workstates/<taskId>.md 并在 summary 前注入；消息链不猜测 taskId
implicit:
  waiting_for: ""
  next_check: ""
  stale_since: ""
links:
  - /Users/admin/GitProjects/msgcode/docs/plan/pl0277.tdo.runtime.workstate-read-injection-at-context-rebuild.md
  - /Users/admin/GitProjects/msgcode/docs/protocol/WORKSTATE.md
  - /Users/admin/GitProjects/msgcode/src/runtime/context-policy.ts
  - /Users/admin/GitProjects/msgcode/test/p7-tk0277-workstate-read-context.test.ts
  - /Users/admin/GitProjects/msgcode/AIDOCS/reports/workstate-readback-trial-260320-heartbeat-rebuild.md
---

# 任务：WORKSTATE 读回接线

## Context

`0276` 已证明 `WORKSTATE` 有恢复增益。

下一步不该直接开 graph/router，而该先做最薄的读回接线：

- 只读
- 只在已有 `taskId` 时发生
- 只作为上下文骨架注入

## Goal / Non-Goals

### Goal

- `assembleAgentContext()` 在 task/heartbeat 续跑时可读取 `WORKSTATE`
- `WORKSTATE` 进入模型上下文，但不覆盖 `summary / checkpoint`

### Non-Goals

- 不为 message 链猜测 `taskId`
- 不新增 `WORKSTATE manager`
- 不新增 graph/router
- 不把 `WORKSTATE` 升格为真相源

## Plan

- [x] 给 `assembleAgentContext()` 增加 `taskId -> WORKSTATE` 只读加载
- [x] 在 dialog/tool-loop 两条模型入口都注入 `WORKSTATE`
- [x] 补回归锁：task 链注入、message 链不误注入、注入顺序在 summary 前

## Acceptance Criteria

1. task/heartbeat 续跑链可读取 `<workspace>/.msgcode/workstates/<taskId>.md`。
2. `WORKSTATE` 作为上下文骨架出现在 `summary` 前。
3. message 链没有 `taskId` 时不做猜测式读取。

## Verification

- `PATH="$HOME/.bun/bin:$PATH" bun test test/p5-6-8-r4b-window-summary-injection.test.ts test/p7-tk0277-workstate-read-context.test.ts test/p5-7-r9-t3-memory-persistence-clear-boundary.test.ts test/p6-agent-run-core-phase3-context-policy.test.ts`
- `./node_modules/.bin/tsc --noEmit`

## Notes

- 这刀只做读取，不做自动写盘。
- 这刀只给 task/heartbeat 续跑链路接线，不给 message 链猜 taskId。
- 这刀证明的是 `WORKSTATE` 可以最薄地进主链，不是 graph/router 已经成立。
- 当前已补一条 compact 后 heartbeat 续跑样本，证明 `WORKSTATE` 真实进入读回主链。

## Links

- Plan: /Users/admin/GitProjects/msgcode/docs/plan/pl0277.tdo.runtime.workstate-read-injection-at-context-rebuild.md
- Protocol: /Users/admin/GitProjects/msgcode/docs/protocol/WORKSTATE.md
- Code: /Users/admin/GitProjects/msgcode/src/runtime/context-policy.ts
- Test: /Users/admin/GitProjects/msgcode/test/p7-tk0277-workstate-read-context.test.ts
- Report: /Users/admin/GitProjects/msgcode/AIDOCS/reports/workstate-readback-trial-260320-heartbeat-rebuild.md
