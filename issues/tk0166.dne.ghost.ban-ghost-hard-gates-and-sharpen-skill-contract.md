---
id: 0166
title: 禁止为 ghost 自作多情加硬拦截并锐化 skill 契约
status: done
owner: agent
labels: [docs, refactor]
risk: low
scope: AGENTS + ghost skill + prompt/test 收口为语义契约优先
plan_doc: docs/plan/pl0166.dne.ghost.ban-ghost-hard-gates-and-sharpen-skill-contract.md
links:
  - issues/0165-ghost-skill-risk-confirmation-guidance.md
  - docs/plan/plan-260310-ghost-os-desktop-plugin-replacement.md
---

## Context

当前方向已经明确：`ghost_*` 的高风险语义优先通过 skill / prompt 约束模型先问用户，而不是新增 confirm gate。用户进一步要求把这条原则写进项目级 AGENTS，并把 `ghost` skill 改成“文笔极度锐利、后果极其严重”的版本，明确安全边界属于语义契约，不属于执行层硬拦截。

## Goal / Non-Goals

- Goal: 在 `AGENTS.md` 写死“不要自作多情写任何代码拦截，优先写更锋利的 skill 契约”。
- Goal: 锐化 `ghost` skill 的高风险动作说明，让风险和责任足够明确。
- Goal: 补回归锁，防止后续又回流 gate 叙事。
- Non-Goals: 不新增任何执行层 gate、token、审批层。
- Non-Goals: 不修改 `ghost-mcp-client`、Tool Bus 或 tool-loop 行为。

## Plan

- [x] 建 issue / plan，冻结“语义契约优先于硬编码拦截”的口径。
- [x] 更新 `AGENTS.md`。
- [x] 锐化 `src/skills/runtime/ghost-mcp/SKILL.md`。
- [x] 更新相关测试与 `docs/CHANGELOG.md`。
- [x] 跑直接相关测试、`npx tsc --noEmit`、`npm run docs:check`。

## Acceptance Criteria

1. `AGENTS.md` 明确禁止为 ghost 自作多情新增硬拦截，优先用 skill 契约。
2. `ghost` skill 用词足够明确地描述高风险动作的后果与责任。
3. 回归锁能防止后续删掉这条口径或改回 gate 叙事。
4. 通过直接相关测试、`npx tsc --noEmit`、`npm run docs:check`。

## Notes

- Docs: `AGENTS.md` 新增“高风险能力先写更锋利的 SKILL 契约，不要自作多情新增代码拦截”口径。
- Docs: `src/skills/runtime/ghost-mcp/SKILL.md` 将高风险动作改写为“语义灾难”叙事，明确“点得准不等于做得对”。
- Tests: `test/p5-7-r38-ghost-mcp-skill-guidance.test.ts` 回归锁同步到新文案。
- Verification:
  - `PATH="$HOME/.bun/bin:$PATH" bun test test/p5-7-r38-ghost-mcp-skill-guidance.test.ts test/p5-7-r3n-system-prompt-file-ref.test.ts`
  - `npx tsc --noEmit`
  - `npm run docs:check`

## Links

- Plan: `docs/plan/plan-260313-ban-ghost-hard-gates-and-sharpen-skill-contract.md`
