---
id: 0197
title: Feishu Inbound Temp File Cleanup
status: pss
owner: agent
assignee: codex
reviewer: user
labels: [feishu, cleanup, housekeeping]
risk: low
scope: 飞书入站资源从临时目录复制进 workspace downloads 后的本地临时文件清理
plan_doc: docs/plan/pl0197.tdo.feishu.feishu-inbound-temp-file-cleanup.md
links:
  - issues/tk0012.pss.feishu.p1.feishu-inbound-attachments-and-channel-provider-boundary.md
---

## Context

- `tk0012` 已完成飞书非文本消息到统一附件主链的映射。
- 当前资源下载先落在 `/tmp/msgcode-feishu-inbound`，再复制到 workspace `downloads/*`。
- 现状缺少临时文件回收，容易在本机积累垃圾文件。

## Goal / Non-Goals

- Goal:
  - 在临时文件成功复制进入 workspace 后及时删除
  - 保持失败时最小诊断事实，不引入新恢复层
- Non-Goals:
  - 不重构 Feishu transport
  - 不新增临时文件管理器/后台清理服务
  - 不重开 `0012`

## Plan

- [x] 确认临时文件生命周期与删除时机
- [x] 在成功复制后删除临时文件
- [x] 失败路径保留最小日志
- [x] 跑定向测试、`tsc`、`docs:check`

## Acceptance Criteria

1. 成功进入 `downloads/*` 的飞书入站附件不再残留临时文件。
2. 失败路径仍保留足够诊断日志。
3. 不新增控制层或后台清理器。
4. `npx tsc --noEmit`、`npm run docs:check` 通过。

## Notes

- 当前临时目录：`/tmp/msgcode-feishu-inbound`
- 正式真相源：`<workspace>/downloads/*`
- 目标是清理成功路径残留，不把失败诊断一起抹掉

## Links

- Plan: `docs/plan/pl0197.tdo.feishu.feishu-inbound-temp-file-cleanup.md`
- Previous issue: `issues/tk0012.pss.feishu.p1.feishu-inbound-attachments-and-channel-provider-boundary.md`
