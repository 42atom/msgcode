---
id: 0204
title: Workspace Heartbeat Alarm And Reflection Mainline
status: open
owner: agent
labels: [runtime, agent, docs]
risk: high
scope: heartbeat 主循环、精确定时唤醒、任务巡检、反思回收、文档真相源对齐
plan_doc: docs/plan/pl0204.pss.runtime.heartbeat-alarm-and-reflection-mainline.md
links:
  - docs/plan/rs0204.dne.agent.heartbeat-cron-reflection-benchmark.md
  - issues/tk0202.pss.agent.p1.shared-skill-pool-and-persona-profile-mainline.md
  - issues/tk0203.pss.agent.p0.doc-first-task-dispatch-and-delivery-supervision.md
created: 2026-03-14
due:
---

## Context

- 用户已经明确提出：主脑不仅要会响应，还要会周期性回看工作目录、检查未完成任务、监督 subagent、在完成后写日记并回收记忆与 skill 改进。
- 现有仓库已经有 `scheduler`、`jobs`、`/task`、`TaskSupervisor`、`subagent` 等积木，但缺一条正式的”主脑节律”主链。
- 对标结果显示：
  - `pi-mono` 在文件唤醒上很薄
  - `openclaw` 在 heartbeat / cron / subagent 分工上最清楚
  - `hermes-agent` 在学习循环叙事上最完整
- msgcode 需要在不加厚控制面的前提下，把这三块收口成自己的主循环。

## Goal / Non-Goals

- Goal:
  - 为每个 workspace 建立正式 heartbeat 主循环
  - 提供精确定时 alarm/cron 唤醒机制
  - 把任务巡检、subagent 监督、反思回收接进同一条主链
  - 让所有状态继续优先落到文件真相源
- Non-Goals:
  - 不新建一个重型 autonomic platform
  - 不把 `issues/` 状态真相源替换成数据库状态
  - 不在本单先做大而全的学习系统

## Progress (Phase 1: 候选实现)

- [x] HEARTBEAT.md 协议文档 (docs/protocol/HEARTBEAT.md)
- [x] HEARTBEAT.md 最小结构模板
- [x] heartbeat tick 集成代码 (src/runtime/heartbeat-tick.ts)
  - ✅ 传递 persona + taskCard 给 subagent runtime
  - ✅ 推进子任务文档状态 (tdo -> rvw -> pss)
  - ✅ 子任务完成后，能按真实 `parentTaskId` 推进父任务到 `rvw`
  - ✅ WATCH_TIMEOUT 超时处理：保持 running 状态 + 提取 taskId + 持续监督
  - ✅ 非 git workspace fallback (fs.renameSync)
  - ✅ `rvw` 子任务会消费现有 Verify 合同，验证通过后才进入 `pss`
- [x] 最小可跑主链冒烟测试 (test/p7-tk0204-heartbeat-tick.test.ts)
  - ✅ D1: 无任务时返回 HEARTBEAT_OK
  - ✅ D2: 有子任务时创建 dispatch 记录
  - ✅ D2b: 验证 persona + taskCard 传递给 runtime
  - ✅ D3: persona 按 board 自动选择
  - ✅ D4: 跳过已存在的 pending dispatch
  - ✅ D5: 超时后能继续监督子代理状态（新增）
- [x] real-test workspace 冒烟证据
  - ✅ 在 `/Users/admin/msgcode-workspaces/real-test` 真实读取 `issues/`
  - ✅ `tk0214` 使用 `frontend-builder` persona 发 dispatch 并落地 `index.html`
  - ✅ `tk0215` 基于同一 `index.html` 继续推进第二张子任务，而不是另起旁路
  - ✅ `dispatch / subagent / issues` 三类真相源都能看到连续推进
  - ✅ 已补回归：Codex 无 JSONL 时 fallback 到 pane，并能靠 done marker 立即收尾
  - ✅ 新增 real-test：`tk0223 -> tk0224 -> tk0225` 证明 `waiting_for` 会先锁住第二张子任务，且父任务只在两张子任务都完成后进入 `rvw`
  - ✅ 新增 real-test：`tk0220 -> tk0221 -> tk0222` 证明第一张子任务推进到 `pss` 后，父任务不会过早结束；第二张子任务完成后父任务进入 `rvw`
- [ ] alarm/cron 协议实现 (Phase 2)
- [ ] reflection 协议实现 (Phase 2)
- [ ] 日志/日报落点 (Phase 2)

## Progress (Phase 1: 已收口)

- [x] 读任务 -> 选 persona -> 发 dispatch -> 子代理执行 -> 回写文档 -> 推进下一单
- [x] 超时后的后续监督能把 dispatch 与子任务文档一起推进
- [x] `pss` 已重新收口为“验证通过”，不再等于“执行臂口头 DONE”
- [x] 验证失败已有出口：第一次失败留 `rvw`，下一次 heartbeat 仍看到同一失败证据则推进 `bkd`
- [x] 父子任务关系、`waiting_for` 顺序、父任务自动推进已被回归和 real-test 锁住
- [x] Codex JSONL 缺失时，watch 收尾仍能靠 pane marker 立即完成
- [x] running 子代理可被 heartbeat 继续追问一次，且 follow-up 会落回 dispatch 真相
- [x] heartbeat 每次 tick 结束后会生成 `.msgcode/STATUS` 只读观测快照，不改协议面真相源
- [x] Phase 1 最小主链已具备 review 证据

## Plan

- [x] 定义 workspace heartbeat 合同：输入、输出、静默 ack、触发频率
- [x] 明确 heartbeat 物理触发策略：
  - 第一版由统一 heartbeat runner 托管
  - 不为每个 workspace 先起独立常驻进程
  - `fsevents` 只作为后续优化，不作为第一版地基
- [x] 明确 heartbeat 如何读取：
  - `HEARTBEAT.md`
  - `issues/*.md`
  - 派单记录
  - subagent 状态
- [x] 明确 `HEARTBEAT.md` 与 `issues/ / .msgcode/wakeups/ / .msgcode/dispatch/` 的真相源边界
- [x] 落一版 heartbeat 协议模板：
  - 读取顺序
  - 静默 ack 口径
  - `HEARTBEAT.md` 最小结构
- [ ] 落一版 alarm 文件协议
- [ ] 明确 reflection 最小回收步骤
- [ ] 明确 reflection review 口径
- [x] 对齐 `/task`、`subagent`、任务看板与主循环的绑定方式

## Acceptance Criteria

### A. 主循环地基

1. 主脑存在一条明确 heartbeat 主循环，而不是只靠用户消息驱动。 ✅
2. heartbeat 与 alarm/cron 分工明确：
   - heartbeat = 周期 awareness / 巡检 / 推进
   - alarm/cron = 精确定时唤醒 (Phase 2)
3. heartbeat 默认读取 workspace 文件真相源，而不是另建隐藏状态层。 ✅
4. 没有需要动作时，heartbeat 能静默结束，不刷屏。 ✅
5. `HEARTBEAT.md` 被明确定义为主脑草稿/提示文件，不承载硬状态真相；与 `issues/` 或 `.msgcode/*` 合同冲突时，一律以后者为准。 ✅
6. 任务完成后，存在最小 reflection 流程，至少覆盖：
   - 日记 (Phase 2)
   - memory candidate (Phase 2)
   - skill candidate (Phase 2)
7. 存在一版 heartbeat 协议模板和一版 alarm 文件协议，能支撑后续实现接线。 (Phase 2)
8. alarm 文件协议明确写清 (Phase 2)
9. reflection candidate 不会自动进入正式 memory / skill；下一轮 heartbeat 必须存在最小审核动作决定其去向。 (Phase 2)

### B. 经理型主脑口径

10. 主脑默认优先派单，而不是自己直接下场执行：
    - 面对可委派任务时，优先走 `dispatch + subagent`
    - 只有明确的小任务才允许主脑亲做
11. `completed` 不能只靠子代理口头回报，必须绑定 artifact、evidence 或明确验收结果。
    - ✅ 当前最薄实现：DONE -> `rvw`，由 heartbeat 跑 Verify 命令后再进 `pss`
    - ✅ 若验证连续失败，则从 `rvw` 推进到 `bkd`，等待主脑或人类决策
12. 主脑不能只写 `.msgcode/dispatch/*.json`，还必须推进对应 `issues/*.md` 状态；必要时继续推进父任务。
13. 主脑对子代理和用户都要忠诚：
    - 对子代理：会派单、巡检、验收、续推
    - 对用户：阻塞、失败、证据不足、需求变化时及时说真话
14. 至少存在一条测试或 real-test 证据，证明主脑不是“只会派一次”，而是会管理完整闭环：
    - 读任务
    - 选 persona
    - 发 dispatch
    - 子代理执行
    - 主脑验收
    - 回写文档
    - 继续推进下一单或如实汇报阻塞
    - ✅ 已有 real-test 证据：`tk0213 -> tk0214 -> tk0215`

### C. 当前 Phase 1 对齐点

15. 该主循环能驱动 `tk0202` 与 `tk0203`，使 persona 装配和 doc-first 派单真正转起来。 ✅
    - ✅ 传递 persona 给 subagent runtime
    - ✅ 传递 taskCard (cwd/acceptance/constraints) 给 subagent runtime
    - ✅ 推进子任务文档状态 (tdo -> pss)
    - ✅ 已补最小父任务推进：父任务 `## Child Tasks` 全部进入 `pss/dne` 后推进到 `rvw`
    - ⚠️ 更复杂的多任务依赖检查仍留到 Phase 2

## Notes

- 这单是主线单。
- 如果没有这条主循环，前面的 persona 与 doc-first dispatch 只能算被动组件，不会自己转。
- 设计目标不是”比别人功能更多”，而是”比别人更薄、更真、更能长期跑”。
- 验收口径不再只是“会不会起 heartbeat”，而是“它是不是一个对用户忠诚、对子代理负责的经理型主脑”。
- Phase 1 已完成最小可跑主链：读任务 -> 选 persona -> 发 dispatch -> 结果可见
- Phase 2 继续补 alarm/cron 和 reflection

## Reflection

### 本轮确认成立

- heartbeat 不是只会“派一次然后停住”：
  - `tk0214` 完成后，主链继续推进了 `tk0215`
  - 两张子任务都落到了同一 workspace 的同一产物路径
- persona 装配已经不只是文档声明：
  - real-test 的两张子任务都以 `frontend-builder` 进入 dispatch
- doc-first 真相源已经真正被消费：
  - heartbeat 扫的是 workspace `issues/`
  - 用户验收看的是 `issues/`、`dispatch`、`subagent` 和真实产物

### 本轮暴露缺口

- Codex 子代理启动链还有环境波动：
  - 第一次 real-test 暴露了目录 trust prompt
  - 第二次又暴露了 Codex JSONL 不稳定存在，导致 responder 不能只押 JSONL
- 这轮又真实暴露了一条旧兼容债：
  - `heartbeat-tick.ts` 里残留 `require("fs")`
  - 导致 dispatch 已完成时，子任务/父任务文件推进报 `require is not defined`
  - 结果是产物已变更，但 `issues/` 真相源没一起推进

### Recent Operating Notes (2026-03-21)

- 子代理更适合窄切片：任务越单一，结果越稳，验收也更清楚。
- 子代理完成不等于主工作树已落地：仍要用 `git log` 和 `git status` 复核真相源。
- 子代理线程位是硬约束：完成后要及时回收，避免占住后续派发空间。

### Current Subagent Readiness Position (2026-03-21)

- 现阶段是“受控可用”，不是稳态可用。
- 更适合窄切片任务，不适合默认长链路多轮复用。
- 当前最大缺口是可见性：主工作树落地、上下文继承、线程位容量。
- 后续需要结合 Codex 子代理深度研究再对照，避免直接照搬产品层设计。

### 本轮补硬

- `watch/marker` 收尾已补回归：
  - `test/p5-7-r37-subagent-runtime.test.ts` 锁住 responder 已返回 done marker 时，subagent runtime 直接完成，不再依赖额外 pane 轮询
  - `test/p5-7-r38-tmux-responder-codex-fallback.test.ts` 锁住 Codex 缺少 JSONL 时，会 fallback 到 pane，并在看到 done marker 后立即返回
- Codex fallback 等待也已收口：
  - `src/tmux/responder.ts` 中等待 JSONL 的时间上限不再固定傻等 15 秒，而是受本次请求 timeout 约束
- 父子任务接线已补最小闭环：
  - heartbeat 创建 dispatch 时，会从子任务 `## Parent Task` 读取真实 `parentTaskId`
  - 父任务会从 `## Child Tasks` / `implicit.waiting_for` 收口依赖
  - 所有子任务进入 `pss/dne` 后，父任务自动推进到 `rvw`
- 真实依赖顺序也已补硬：
  - real-test 第一轮曾真实暴露 `tk0222` 在 `waiting_for=tk0221` 下被错误先派发
  - 现已补上 `waiting_for` 调度过滤，修复后 real-test 中 `tk0224` 会先于 `tk0225` 被派发，且 `tk0223` 只在两张子任务都完成后进入 `rvw`
- 真实文件推进兼容债也已补硬：
  - `heartbeat-tick.ts` 已清掉主链里的 `require("fs")`
  - 新增回归 `D3c`：非 git workspace 下，dispatch 完成后必须同时推进子任务与父任务
  - real-test 复跑后，`tk0221` 已进入 `pss`，`tk0220` 已进入 `rvw`

### 收口判断

- 现在成立的是：
  - 读任务 -> 选 persona -> 发 dispatch -> 子代理执行 -> watch 自动收尾 -> 文档真相源推进 -> 继续推进下一单
- 现在未做的是：
  - `alarm/cron`
  - `reflection`
  - 日志/日报落点
- 这些属于 Phase 2，不再阻塞当前 Phase 1 收口

## Final Review Position

- 这张单当前应按 `Phase 1 已完成` 进入 `rvw`
- 当前 review 关注点不再是“主链是否成立”，而是：
  - 是否认可把 `alarm/cron` 与 `reflection` 留到 Phase 2
  - 是否认可 real-test + 回归已足以证明 heartbeat 主循环最小闭环成立
- Phase 2 已拆相，不再继续挂在本单里滚动补丁：
  - [tk0216.pss.runtime.p1.alarm-cron-file-contract-and-wake-mainline.md](/Users/admin/GitProjects/msgcode/issues/tk0216.pss.runtime.p1.alarm-cron-file-contract-and-wake-mainline.md)
  - [tk0217.pss.runtime.p1.reflection-candidate-review-and-daily-log-mainline.md](/Users/admin/GitProjects/msgcode/issues/tk0217.pss.runtime.p1.reflection-candidate-review-and-daily-log-mainline.md)

## Review Packet

- 回归：
  - `test/p7-tk0204-heartbeat-tick.test.ts`
  - `test/p5-7-r37-subagent-runtime.test.ts`
  - `test/p5-7-r38-tmux-responder-codex-fallback.test.ts`
- 真实 workspace：
  - `/Users/admin/msgcode-workspaces/real-test`
- 真实父子冒烟：
  - `/Users/admin/msgcode-workspaces/real-test/issues/tk0213.rvw.runtime.real-test-heartbeat-persona-dispatch-smoke.p0.md`
  - `/Users/admin/msgcode-workspaces/real-test/issues/tk0214.pss.web.p1.real-test-create-html-page.md`
  - `/Users/admin/msgcode-workspaces/real-test/issues/tk0215.pss.web.p1.real-test-theme-switch-followup.md`
- 真实依赖顺序补证据：
  - `/Users/admin/msgcode-workspaces/real-test/issues/tk0223.rvw.runtime.real-test-dependency-order-after-fix.p0.md`
  - `/Users/admin/msgcode-workspaces/real-test/issues/tk0224.pss.web.real-test-order-proof-a.p1.md`
  - `/Users/admin/msgcode-workspaces/real-test/issues/tk0225.pss.web.real-test-order-proof-b.p1.md`
- 真实双子任务父单补证据：
  - `/Users/admin/msgcode-workspaces/real-test/issues/tk0220.rvw.runtime.real-test-parent-two-child-sequencing.p0.md`
  - `/Users/admin/msgcode-workspaces/real-test/issues/tk0221.pss.web.real-test-add-proof-banner.p1.md`
  - `/Users/admin/msgcode-workspaces/real-test/issues/tk0222.pss.web.real-test-add-proof-footer.p1.md`
- 产物：
  - `/Users/admin/msgcode-workspaces/real-test/index.html`

## Links

- Benchmark: `docs/plan/rs0204.dne.agent.heartbeat-cron-reflection-benchmark.md`
- Related: `issues/tk0202.pss.agent.p1.shared-skill-pool-and-persona-profile-mainline.md`
- Related: `issues/tk0203.pss.agent.p0.doc-first-task-dispatch-and-delivery-supervision.md`
- Runtime: `src/runtime/heartbeat-tick.ts`
- Test: `test/p7-tk0204-heartbeat-tick.test.ts`
- Real Test Parent: `issues/tk0213.pss.runtime.p0.real-test-heartbeat-persona-dispatch-smoke.md`
