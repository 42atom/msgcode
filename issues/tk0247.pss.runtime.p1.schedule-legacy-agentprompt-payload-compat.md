---
id: 0247
title: Schedule Legacy AgentPrompt Payload Compat
status: pass
owner: agent
assignee: codex
reviewer: user
labels: [runtime, schedule]
why: 真实 cron jobs.json 里仍有历史 agentPrompt payload，当前 runner 只认 chatMessage/tmuxMessage，导致 schedule job 持续失败
scope: 只补 jobs 读路径对历史 agentPrompt 的薄兼容；不回退新 schedule 投影合同
risk: low
accept: 历史 jobs.json 中的 agentPrompt schedule 可继续执行并产生日志；新 schedule 写路径仍保持 chatMessage/tmuxMessage
implicit:
  waiting_for: ""
  next_check: ""
  stale_since: ""
links:
  - /Users/admin/GitProjects/msgcode/src/config/schedules.ts
  - /Users/admin/GitProjects/msgcode/src/jobs/runner.ts
  - /Users/admin/GitProjects/msgcode/src/jobs/types.ts
  - /Users/admin/GitProjects/msgcode/test/p6-agent-run-core-phase1.test.ts
---

## Context

- 现场日志已出现真实失败：`source=schedule error=不支持的 payload.kind`
- 现代码的新投影已经写 `chatMessage`
- 但真实 `~/.config/msgcode/cron/jobs.json` 里还存在历史 `agentPrompt`

## Goal

- 让历史 `agentPrompt` schedule 继续执行
- 不回退当前 schedule 新合同

## Non-Goals

- 不把新投影再写回 `agentPrompt`
- 不新增 schedule 迁移平台
- 不补第二套 payload 抽象层

## Goal / Non-Goals

- Goal:
  - 让历史 `agentPrompt` schedule 在不改 `jobs.json` 的情况下继续可执行
  - 保持新 schedule 投影继续写 `chatMessage`
- Non-Goals:
  - 不回退 schedule 新合同
  - 不新增迁移控制面
  - 不把 payload 体系再做厚

## Plan

- [x] runner 读取时兼容历史 `agentPrompt`
- [x] 补一条最小回归测试
- [x] 验证不影响现有 `chatMessage` 主链

## Acceptance Criteria

1. 历史 `agentPrompt` payload 的 job 可以被 `executeJob()` 正常执行。
2. 现有 `chatMessage` schedule 主链不回退。
3. 不新增新写路径、迁移器或第二层 payload 抽象。

## Notes

- 现场真相是读路径漂移，不是写路径回退。
- 最薄修法就是在 runner 兼容一次历史 payload。

## Result

- `executeJob()` 现在会把历史 `agentPrompt` 当作直接消息 payload 处理。
- 新 schedule 投影仍保持 `chatMessage` / `tmuxMessage`，没有回退。
- 已补最小回归，锁住历史 `jobs.json` 兼容读路径。

## Verification

- `PATH="$HOME/.bun/bin:$PATH" bun test test/p6-agent-run-core-phase1.test.ts test/p5-7-r17-scheduler-pointer-only.test.ts`
- `./node_modules/.bin/tsc --noEmit`
- `npm run docs:check`

## Links

- Related: `src/config/schedules.ts`
- Related: `src/jobs/runner.ts`
- Related: `src/jobs/types.ts`
- Test: `test/p6-agent-run-core-phase1.test.ts`
