---
id: 0379
title: UI Protype Prototype Research Surface Restoration
status: pss
owner: user
assignee: codex
reviewer: user
labels: [product, ui, docs]
why: `ui-protype` 现在混进了现役读面联通，必须把它收回原型研究面，避免和正式 UI 主线继续互相污染
scope: 只收 `ui-protype` 的职责口径；不改代码，不改视觉，不改 `src`
risk: low
accept: 文档里能明确看出 `ui-protype` 只承载原型研究面，正式 UI 落地到 `src`
implicit:
  waiting_for: ""
  next_check: ""
  stale_since: ""
links:
  - /Users/admin/GitProjects/msgcode/issues/tk0377.pss.product.p1.desktop-main-window-readonly-thread-surface-mainline.md
  - /Users/admin/GitProjects/msgcode/docs/plan/pl0377.pss.product.desktop-main-window-readonly-thread-surface-mainline.md
  - /Users/admin/GitProjects/msgcode/docs/plan/rf0001.rvw.product.electron-current-ui-constraints.md
  - /Users/admin/GitProjects/msgcode/issues/tk0378.pss.product.p1.readonly-thread-surface-implementation.md
  - /Users/admin/GitProjects/msgcode/docs/plan/pl0211.tdo.agent.active-mainline-execution-index.md
---

# 任务：恢复 `ui-protype` 为原型研究面

## Context

`ui-protype` 现在混进了现役读面联通，必须把它收回原型研究面，避免和正式 UI 主线继续互相污染。

## 为什么现有文件不能直接承载这个职责

`tk0377` 是正式 UI 主线，只管现役落点，不该同时承担原型收口。

`tk0378` 是已收口的实现单，不能回头改写职责边界。

`rf0001` 只是参考，不是任务。

`pl0211` 只是导航，不是真相源。

所以要单独开一张最薄任务单，只把 `ui-protype` 的定位收回原型研究面。

## Goal / Non-Goals

### Goal

- 明确 `ui-protype` 只保留原型研究面
- 明确正式 UI 落地到 `src`

### Non-Goals

- 不改视觉方案
- 不改代码
- 不碰 `src` 落点

## Plan

- [x] 把 `ui-protype` 的职责口径收回原型研究面
- [x] 把正式 UI 落地口径写到 `src`
- [x] 去掉任何把 `ui-protype` 写成现役实现面的说法

## Acceptance Criteria

1. 文档里能一眼看出 `ui-protype` 不是正式 UI 落点。
2. 文档里能一眼看出正式 UI 落地到 `src`。
3. 这张单不扩到代码和视觉。

## Notes

- 这张单只收 `ui-protype` 的职责口径。
- 正式 UI 落点只写到 `src`。
- 不改任务边界。

## Links

- [tk0377.pss.product.p1.desktop-main-window-readonly-thread-surface-mainline.md](/Users/admin/GitProjects/msgcode/issues/tk0377.pss.product.p1.desktop-main-window-readonly-thread-surface-mainline.md)
- [pl0379.pss.product.ui-protype-prototype-research-surface-restoration.md](/Users/admin/GitProjects/msgcode/docs/plan/pl0379.pss.product.ui-protype-prototype-research-surface-restoration.md)

## 当前 pass 口径

这张单要求的职责边界已经成立：

- `ui-protype/` 回到原型研究面
- 正式 UI 已明确落到 `src/`
- `tk0380` 也已把原型恢复实现收口

所以它不再是待做事项，文档真相已经收直并通过 review。
