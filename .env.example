# msgcode 配置文件
#
# 推荐使用 msgcode init 命令自动创建此文件
# 也可手动复制到 ~/.config/msgcode/.env 后编辑

# ============================================
# 使用者信息（白名单）
# ============================================
# 你的 iMessage 邮箱（只有你发的消息 bot 才会响应）
MY_EMAIL=

# 电话号码（可选）
# MY_PHONE=+8613800138000

# ============================================
# 群组路由配置
# ============================================
# 格式: GROUP_<群组名>=<chatGuid>:<项目路径>:<bot类型>
# chatGuid 通过 msgcode init 自动获取，或手动运行 npm run get-chats
#
# 示例（取消注释后填写你的实际值）：
# GROUP_CODE_BOT=iMessage;+;your-guid:/Users/<you>/path/to/project:code
# GROUP_IMAGE_BOT=iMessage;+;your-guid:/Users/<you>/path/to/images:image

# ============================================
# 默认配置
# ============================================
# 默认群组名称（对应上面的 GROUP_xxx 中的 xxx 部分）
# msgcode init 会自动设置为 DEFAULT_GROUP=default
DEFAULT_GROUP=

# ============================================
# 日志配置
# ============================================
# 日志级别 (debug | info | warn | error)
LOG_LEVEL=info

# 日志文件路径（可选）
# LOG_PATH=~/.config/msgcode/log/msgcode.log

# 禁用文件日志（可选）
# LOG_FILE=false

# ============================================
# iMessage Provider 配置
# ============================================
# Provider 选择: sdk (默认) | imsg
# IMESSAGE_PROVIDER=sdk

# imsg 二进制路径（当 IMESSAGE_PROVIDER=imsg 时必须）
# 推荐使用源码构建产物，而非 brew 安装版本
# 构建方法: ./scripts/build-imsg.sh
# 校验方法: ./scripts/verify-imsg.sh
# IMSG_PATH=/Users/<you>/msgcode/vendor/imsg/v0.4.0/imsg

# ============================================
# 高级配置（可选）
# ============================================
# 暂无高级配置
