# MLX Lab 实验结果

> 实验时间: YYYY-MM-DD HH:MM:SS
> 模型: GLM4.7 Flash (MLX)
> 模型路径: `/Users/admin/Models/glm-4.7-flash-chat-mlxf16/`

---

## 实验配置

| 参数 | 值 |
|------|-----|
| 服务器 | `mlx_lm.server` |
| 基础 URL | `http://127.0.0.1:18000` |
| Temperature | `0` |
| Top P | `1` |
| Max Tokens | `64` (基础和回灌测试) / `256` (闭环测试) |

---

## 测试结果

### 1. 基础响应测试 (`probe-basic.sh`)

**通过门槛**: `basic_pass >= 5/5`

**结果**: `basic_pass = ?/5`

**状态**: `?`

---

### 2. Tool Role 回灌测试 (`probe-tool-role.sh`)

**通过门槛**: `tool_role_pass >= 9/10`

**结果**: `tool_role_pass = ?/10`

**状态**: `?`

**失败样本**:
```
(实验时填写)
```

---

### 3. 工具闭环测试 (`probe-tool-loop.sh`)

**通过门槛**: `tool_loop_pass >= 9/10`

**结果**: `tool_loop_pass = ?/10`

**状态**: `?`

**失败样本**:
```
(实验时填写)
```

---

## 统计表

| 测试项 | 通过数/总数 | 通过率 | 状态 |
|--------|-------------|--------|------|
| 基础响应 | ?/5 | ?% | ? |
| Tool Role | ?/10 | ?% | ? |
| 工具闭环 | ?/10 | ?% | ? |

---

## 污染检查

- [ ] 无 `` ``` `` 污染
- [ ] 无 `<function_calls>` 污染
- [ ] 无 `<tool>` 污染

---

## 最终结论

- [ ] **可迁移 autonomous** - 所有测试通过，可考虑将 `tooling.mode` 默认改为 `autonomous`
- [ ] **不可迁移（保持 explicit）** - 测试未通过，继续使用 `explicit` 模式

**结论**: `?`

---

## 原始响应摘要

### 基础响应测试

```
(实验时填写)
```

### Tool Role 回灌测试

```
(实验时填写)
```

### 工具闭环测试

```
(实验时填写)
```

---

## 备注

```
(实验时填写)
```
