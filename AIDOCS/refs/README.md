# refs 历史参考说明

本目录中的 `imessage-kit`、`advanced-imessage-kit` 与相关文章仅作为**历史参考资料**保留。

当前边界：

- 它们**不是** `msgcode` 现役 runtime 的实现来源
- 它们**不是**新功能设计的默认依赖
- 它们只用于回顾 iMessage 阶段的历史技术背景、协议研究与测试白名单上下文

当前正式主链以仓库 `src/`、`issues/`、`docs/design/` 和 `docs/CHANGELOG.md` 为准。

如果需要查看已退役的 `imsg` 运行时快照，请看：

- [docs/archive/retired-imsg-runtime/](/Users/admin/GitProjects/msgcode/docs/archive/retired-imsg-runtime)
